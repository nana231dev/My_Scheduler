# 📋 My_Scheduler 프로젝트 현황 정리 (2026-09-11 기준)

## 1. 프로젝트 개요
- **목표:** 월간/주간/일간 스케줄러 + 음력/24절기/공휴일 + 엑셀 + 뉴스 수집 + 증권 시세 + 단어장/생활용어 DB
- **기술 스택:** Python 3.14 / tkinter + ttkbootstrap(darkly) / SQLite / pandas / korean_lunar_calendar / urllib(RSS)
- **진입점:** `python 10yp_2.py` (유일 실행 파일, 1,682줄)

## 2. 파일 구조
```
My_Scheduler/
├── 10yp_2.py              ← 메인 앱 (모든 뷰 통합)
├── scheduler.db           ← 통합 DB (13개 테이블)
├── config.json            ← API 키 4종 + 테마 저장
├── requirements.txt       ← 의존성
├── migrate_db.py          ← DB 이관 도구
├── My_Scheduler_Plan*.md  ← 계획 문서
├── 기념일.xlsx
├── core/                  ← 공통 모듈 (8개)
│   ├── __init__.py
│   ├── solar_terms.py     ← 24절기 (24줄)
│   ├── db_manager.py      ← DB CRUD (591줄)
│   ├── news_fetcher.py    ← 뉴스 RSS 수집 (186줄, 21소스·9분야)
│   ├── market_fetcher.py  ← 금융원 OpenAPI (111줄, Mock 폴백)
│   ├── word_data.py       ← 단어 시드 (358줄: 영어300/일본어20/중국어20)
│   ├── phrase_data.py     ← 문장 시드 (359줄: 영어300/일본어20/중국어20)
│   └── config_manager.py  ← config.json 로드/저장 (57줄)
└── archive/               ← 구버전 4개 + DB 백업 + 도구/검증 스크립트
```

## 3. 기능 목록
| # | 탭 | 기능 |
|---|---|---|
| 1 | 월간/주간 | 달력, 음력·절기·공휴일, 주간 요약(W-YYYY-MM-DD), 테마 배경색 |
| 2 | 공부 | 과목별 노트/단어·용어/매일 진도 체크 |
| 3 | 단어장 | 언어별 단어 CRUD (8필드), 검색 |
| 4 | 생활용어 | 언어별 문장 CRUD (4필드), 검색 |
| 5 | 뉴스 | RSS 21소스·9분야, 검색 수집, 저장/삭제, 브라우저 열기 |
| 6 | 증권 | 종목 검색, 저장/삭제, 상세(투자자별 매매동향)+차트 |
| 7 | 설정 | 테마 변경, 금융원 API 키 4종 관리(config.json) |
| 8 | 사이드바 | Year Progress 일자형, 이번달 일정, D-Day |

## 4. 검증 결과 (2026-09-11)
- `py_compile`: 9개 파일 전부 OK
- 앱 import: SchedulerApp + 뷰 13개 메서드 전부 OK
- 시장: MarketFetcher Mock 5건, config 키 4종 로드 OK
- 뉴스: 21소스·9분야 OK
- DB: 13개 테이블 정상
  - wordbook 340, phrases 340, anniversaries 11, schedules 1, ddays 2, saved_news 4, saved_markets 4

## 5. 남은 작업/참고
- 금융원 API 실데이터: 현재 키가 있으나 응답 포맷 불일치 → Mock 모드 유지 중 (`get_stock_detail`이 에러 시 Mock 폴백)
- config.json의 `fss_stock` 키가 앱 시작 시 `self.api_key`로 자동 로드됨
- 뉴스 소스 추가: `core/news_fetcher.py`의 `NEWS_SOURCES`
- 증권 Mock 확장: `core/market_fetcher.py`의 `_mock()` / `_mock_detail()`
