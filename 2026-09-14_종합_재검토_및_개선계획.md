# 🔍 My_Scheduler 종합 재검토 및 개선 계획 (2026-09-14)

> 📌 **이 문서:** 2026-09-14 기준 전체 프로젝트 재검토 + 발견된 문제점 + 전문가 시선 진행 제안 + git 상태 정리
> 이전 문서: `2026-09-13_전수검사_및_개선계획.md` → 이 문서(2026-09-14)가 최신
>
> **✅ 2026-09-14 2차 세션 완료:** 검색어 전달(likeItmsNm+30일 제한)·한글 보존·`_FIELD_ALIASES` 누락 복원·
> stdout 래퍼 제거·`run_app.bat` 추가·지수/일반상품/금융회사 폐기 실측 확정·except 26건 인벤토리.
> 최종 검증: **컴파일 2/2 OK, unittest 27/27 OK, 실데이터 검색 실측 OK** (`Temp/verify_0914.json`)
>
> **✅ 2026-09-14 3차 세션 완료:** except Exception 26건 전량 정리(구체 예외 축소 + 의도적 경계 4곳 noqa 문서화)·
> **테스트 27→34건**(가계부/저장종목/증권 유틸 추가)·로깅 파이프라인 검증(app.log 기록 확인 — 0바이트는 시작 시 이벤트 부재가 정상)·
> **뷰 분리 1단계: `ui/` 패키지 신설 + DailyEditor 추출** (`ui.daily_editor`, 75줄) +
> **GUI 스모크 ALL_OK**(앱 생성 → 9개 탭 전체 전환, 실데이터 검색 로그 확인).
> 최종 검증: **컴파일 OK, unittest 34/34 OK, GUI 스모크 ALL_OK** (`Temp/unittest_0914.json`, `Temp/gui_smoke_result.json`)
> ⏸ git push: 로컬 커밋 완료, 원격 저장소 생성 후 사용자가 push 예정 (보류 합의)

---

## 1. 📌 한눈에 보기 (현재 상태 요약)

| 항목 | 상태 |
|---|---|
| **진입점** | `python 10yp_2.py` (단일 파일, **3,480줄 / 187KB**) |
| **구조** | `10yp_2.py` + `core/` 19개 모듈 + `tests/test_core.py` (27건) |
| **DB** | `scheduler.db` (SQLite, 16개 테이블) |
| **증권 실데이터** | ✅ 주식 시세 실측 확인 (`GetStockSecuritiesInfoService_V2/getStockPriceInfo_V2` 정상 응답) |
| **증권 키** | ✅ `config.json`에 `fss_stock` 반영 (config는 git 제외 대상) |
| **git** | ⚠️ 로컬 저장소 초기화 + 루트 커밋 완료 / 🔴 원격 저장소(GitHub repo) 아직 미생성 → push 불가 상태 |
| **컴파일/테스트** | ✅ 컴파일 22/22, unittest 27/27 (문서 기준) |

---

## 2. 🎯 오늘(2026-09-14)까지 완료된 작업

### 2-0. 2차 세션 코드 수정 + 실측 (2026-09-14 오후)

| 작업 | 내용 | 검증 |
|---|---|---|
| **검색어 전달 수정** | `search_stocks(kw)`가 기존에 kw를 무시(전체 목록 반환) → 이제 공공데이터포털 **`likeItmsNm`(부분일치)** 파라미터로 전달. `itmsNm`은 전체일치라 "삼성"→0건이었음 | ✅ 실측: "삼성" 검색 totalCount=494, 첫 항목 삼성화재 |
| **최신 데이터 우선** | likeItmsNm만 쓰면 전 기간(39,842건) 반환 → **`beginBasDt`/`endBasDt` 최근 30일 제한** 추가 (실측: 내림차순 정렬 확인, 30일=494건) | ✅ 실측 |
| **한글 보존** | `_clean_kor`이 한글 전체 제거(ASCII만 허용) → 정상 유니코드 한글 보존 + U+FFFD/제어문자만 제거로 재작성 | ✅ "삼성전자" 보존 확인 |
| **`_FIELD_ALIASES` 누락 복원** | `normalize_fss_response`가 참조하던 매핑 표가 정의 없이 누락돼 있었음(NameError, 유닛테스트 1건 실패 원인) → 12키 매핑 정의 추가 | ✅ unittest 27/27 |
| **stdout 래퍼 제거** | 모듈 임포트 시 `sys.stdout` 재구성 코드가 Temp 진단 스크립트 "I/O operation on closed file" 오류 유발 → 제거 | ✅ |
| **`run_app.bat` 추가** | `chcp 65001` + `PYTHONIOENCODING=utf-8`로 콘솔 한글 깨짐 근본 해결 | 배치 파일 생성 |
| **지수/일반상품/금융회사 폐기 확정** | 유효 키로 14개 service/op 조합 실측 → **전부 `NO_OPENAPI_SERVICE_ERROR`(서비스 없음/폐기)**. 공공데이터포털에서 대체 서비스 재신청 필요 | ✅ 14조합 실측 |
| **except Exception 인벤토리** | 실측 총 26건 (10yp_2.py 22 / db_manager 1 / exceptions 1[의도적] / market_fetcher 1 / news_fetcher 1) → `Temp/except_inventory.txt` | ✅ |

**최종 회귀 검증:** 컴파일 2/2 OK · **unittest 27/27 OK** · 실데이터 검색 실측 OK (`Temp/verify_0914.json`)

### 2-1. 문서 정리 (금일)
- `2026-09-11_종합_개발계획.md` — 증권 실데이터 연동 현황 반영
- `2026-09-12_전체_진행상황_정리.md` — 최신 상태 반영
- `2026-09-13_전수검사_및_개선계획.md` — git 푸시 직전 상태 반영
- **`2026-09-14_종합_재검토_및_개선계획.md` (본 문서) 신규 작성**

### 2-2. 증권 실데이터 연동 (금일/전일)
- 공공데이터포털(금융위원회) `GetStockSecuritiesInfoService_V2` 계열 실측 완료
  - **주식 시세** `getStockPriceInfo_V2` — ✅ 실데이터 정상 응답 확인 (totalCount 2874)
  - 응답 필드 15종 실측: `basDt/srtnCd/isinCd/itmsNm/mrktCtg/clpr/vs/fltRt/mkp/hipr/lopr/trqu/trPrc/lstgStCnt/mrktTotAmt`
  - **일반상품시세** `getOilPriceInfo_V2` — 오퍼레이션 응답은 받음 (키 등록 이슈 확인됨)
  - **지수시세** — 서비스 식별자/권한 미정 (별도 활용신청 필요 가능성)
  - **금융회사** — 서비스 식별자/권한 미정
- `core/market_fetcher.py`에 공공데이터포털 전용 호출 + UI 매핑(`_gokr_to_ui`) 추가

### 2-3. 숫자 입력부 쉼표 대응 (금일 — `ValueError: could not convert '120,000'` 수정)
- 증권 저장(`_save_selected_market`), 수정(`do_update`), 투자일기 입력(`add`)에 쉼표 제거 후 float 변환 적용
- `core/db_manager.py` `add_investment`에도 안전하게 쉼표 제거 + 0 폴백 적용

### 2-4. git 환경 구축 (금일)
- **Git for Windows 직접 설치 없이, GitHub Desktop 번들 git 사용** (`C:\Users\jusin\AppData\Local\GitHubDesktop\app-3.6.5\resources\app\git\cmd\git.exe`)
- 로컬 저장소 초기화 + `main` 브랜치 + 루트 커밋 완료 (34개 파일, 11,598줄)
- **🔴 아직 실패:** `git push` 불가 — **GitHub 원격 저장소(https://github.com/nana123-create/My_Scheduler)가 GitHub에 생성되어 있지 않음** (`git ls-remote origin` 빈 응답 / GitHub 웹 404 확인)

---

## 3. 🔴 발견된 문제점 (전문가 재검토)

### 3-1. 중대 (블로커)
| # | 문제 | 상태 |
|---|---|---|
| B1 | **GitHub 원격 저장소 미생성** → push 불가 | 🔴 차단 |
| B2 | `config.json`의 `fss_stock`에 실제 키 반영됨(금일 완료) — **그러나 이전까지 빈 키로 Mock만 동작** → 사용자는 그동안 실데이터를 볼 수 없었음 | ✅ 해소 |
| B3 | 지수/일반상품/금융회사 서비스는 여전히 **서비스 식별자·권한 미확정** | ⏳ 진행 중 |

### 3-2. 중요 (장기 위험)
| # | 문제 | 상태 |
|---|---|---|
| M1 | **`10yp_2.py` 단일 파일 3,480줄** — 유지보수 어려움 | 안정화 후 `ui/` 분리 검토 |
| M2 | **Windows 콘솔 인코딩** — 한글 문자열이 콘솔/출력 파일에서 `?`/`??`로 깨짐 반복 (매 세션 재발) | 환경 설정 필요 |
| M3 | **시가총액 표기 단위 제거** — 화면에서 `221.43`(조 단위 표기 없음)처럼 표기되어 혼동 가능 | UI 표기 보완 필요 |
| M4 | **`market_fetcher.py`의 `_clean_kor`이 한글을 전부 제거** — 종목명이 영어/숫자만 남을 위험 | 한글 정상 보존 검증 필요 |

### 3-3. 일반 (코드 품질)
| # | 문제 |
|---|---|
| G1 | `except Exception` 광범위 사용 (74건) — 오류 삼킴이 많음 |
| G2 | `Temp/` 확인 스크립트·진단 파일 다수 (docx 4종 + verify/diag 스크립트 다수) — gitignore 대상이라 push에 안 잡힘 |
| G3 | `archive/logs/app.log` 0바이트 — 로그 확인 어려움 (앱 실행 직후 종료 가능성) |
| G4 | 테스트 커버리지 아직 core 중심 (27건) — 가계부/증권 저장목록/터미널 UI 테스트 부족 |

---

## 4. 🧑‍💻 전문가 시선 — 무엇을 어떻게 진행할 것인가 (제안)

### 4-1. 우선순위 1순위 — git 저장소 생성 + push (오늘 중)
1. **GitHub에 저장소 생성**
   - 방법 A(권장): **GitHub 웹사이트** 접속 → `New Repository` → 이름 `My_Scheduler`, Public/Private 선택, README 생성 안 함 → Create
   - 방법 B: GitHub Desktop → `File > Add Local Repository` → 이 폴더 선택 → `Publish repository`
2. **push 수행** (이미 로컬 커밋 있음)
   ```
   git push -u origin main
   ```
   - push 시 인증: 이미 GitHub Desktop에 로그인돼 있다면 자격 증명 관리자(Windows 자격 증명) 경유로 자동 처리
3. **검증** — GitHub 웹에서 커밋(03eef96) 확인

### 4-2. 우선순위 2순위 — 실데이터 화면 안정화
1. `config.json`의 `fss_stock` 키 반영 확인 후, `get_stock_list()` 실데이터가 화면에 뜨는지 실행 검증
2. `search_stocks(keyword)`가 실제 검색어를 서버에 전달하도록 수정
   - 지금은 `search_stocks()`가 `get_stock_list()`로 고정돼 있어 검색어 무시 → 개선 필요
3. 지수/일반상품/금융회사는 **따로 활용신청이 이미 있다면 키를 채우고 검증**, 아니면 서비스 조사를 우선
4. 종목명 한글 `_clean_kor` 정상화 (한글 보존)

### 4-3. 우선순위 3순위 — 운영 안정성
1. Windows 한글 인코딩 환경 설정:
   - `PYTHONIOENCODING=utf-8` 또는 `chcp 65001` 시작 배치 파일 제공
2. `archive/logs/app.log` 0바이트 원인 확인 — 앱 시작 즉시 크래시 여부 확인
3. 데이터 백업/복원이 이미 있으니, **gitignore에 `backup/`, `data/images/` 확인** 후 push 안전성 확보

### 4-4. 우선순위 4순위 — 코드 품질 (중기)
1. `10yp_2.py` 뷰 파일 분리(월간/주간/일간/공부/뉴스/증권/가계부/설정) — 단계적
2. `except Exception` 축소 + 구체 예외 지정
3. 테스트 확장: 가계부·투자일기·증권 저장목록 27 → 40+
4. `core/db_manager.py` 주석 인코딩 복구

---

## 5. ⚙️ 기술 스택 및 실행 환경 (기록)

| 항목 | 값 |
|---|---|
| Python | 3.14 |
| GUI | tkinter + ttkbootstrap(darkly) |
| DB | SQLite (`scheduler.db`) |
| 증권 API | 공공데이터포털 금융위원회 `GetStockSecuritiesInfoService_V2` |
| 증권 Endpoint | `https://apis.data.go.kr/1160100/GetStockSecuritiesInfoService_V2/getStockPriceInfo_V2` |
| 인증키 | `config.json` → `fss_stock` (git 제외) |

---

## 6. 🗓 오늘 이후 바로 실행할 일 (체크리스트)

- [x] GitHub에 `My_Scheduler` 저장소 생성 (사용자 완료 — private 추정)
- [ ] `git push -u origin main` 실행 → 원격 커밋 확인 (⏸ 사용자가 GitHub Desktop에서 진행 예정)
- [x] 증권 실데이터 검색 실측 — likeItmsNm+30일 제한, GUI 스모크에서 API 호출 로그까지 확인
- [x] `search_stocks(keyword)` 검색어 전달 수정
- [x] `_clean_kor` 한글 보존 검증
- [x] `archive/logs/app.log` 0바이트 원인 확인 — 파이프라인 정상, 시작 시 기록 이벤트가 없었을 뿐 (`Temp/log_smoke.json`)
- [x] except Exception 정리 — 26건 전량 처리(축소 17건 + 의도적 경계 문서화 9건), 잔여 broad는 경계 역할로 noqa 주석 명시
- [x] 테스트 확장 — 27 → **34건** (가계부 2·저장종목 1·증권 유틸 4 추가)
- [x] 뷰 분리 1단계 — `ui/` 패키지 + DailyEditor 추출 + GUI 전체 탭 스모크 ALL_OK
- [ ] (다음 단계) 뷰 분리 2단계 — 뷰 메서드 그룹을 Mixin 파일로 단계 이관(월간/주간/일간 → 공부 → 뉴스/증권/가계부/설정), 각 단계마다 GUI 스모크 재실행
- [ ] (다음 단계) 지수/일반상품/금융회사 대체 서비스 공공데이터포털 재신청 → 새 엔드포인트 연동