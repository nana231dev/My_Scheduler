# 📋 My_Scheduler 프로젝트 정리 (2026-09-06 기준)

## 1. 프로젝트 개요
- **목표:** 월간/주간/일간 스케줄러 + 음력/24절기/공휴일 표시 + 엑셀 연동 + 뉴스 수집 + 증권 시세 + 단어장/생활용어 DB
- **기술 스택:** Python / tkinter + ttkbootstrap / SQLite / pandas / korean_lunar_calendar / urllib (RSS)

## 2. 파일 현황 (2026-09-06 정리 후)

### 현재 활성 파일 (루트)
| 파일 | 규모 | 설명 |
|---|---|---|
| `10yp_2.py` | 2,020줄 | **유일한 실행 버전** (모든 기능 통합) |
| `scheduler.db` | - | 통합 DB (최신 스키마) |
| `config.json` | - | API 키/테마 설정 파일 |
| `requirements.txt` | - | 의존성 명세 |
| `migrate_db.py` | - | DB 이관 도구 (재사용 가능) |
| `My_Scheduler_Plan.md` | - | 개발 로드맵 문서 |
| `My_Scheduler_Plan_CrossPlatform.md` | - | 크로스플랫폼 확장 계획 문서 |
| `기념일.xlsx` | - | 기념일 가져오기용 엑셀 데이터 |

### `core/` 폴더 (공통 모듈)
| 파일 | 설명 |
|---|---|
| `__init__.py` | 패키지 초기화 |
| `solar_terms.py` | 24절기 계산 |
| `db_manager.py` | DB 관리 (모든 테이블 CRUD) |
| `news_fetcher.py` | RSS/Atom/RDF 뉴스 수집 (21개 소스 · 9개 분야) |
| `market_fetcher.py` | 금융원 OpenAPI 주가/지수/상품/회사 정보 |
| `word_data.py` | 영어 300/일본어 20/중국어 20 단어 시드 |
| `phrase_data.py` | 영어 300/일본어 20/중국어 20 문장 시드 |
| `config_manager.py` | config.json 로드/저장 (API 키, 테마) |

### `archive/` 폴더 (구버전 + 백업)
구버전 4개 (`my_scheduler.py`, `my_scheduler_old.py`, `my_scheduler_fixed.py`, `10YP.py`) + DB 백업 2개 + 도구 스크립트 + `vocab_tools/`

### 정리 완료 사항
- ✅ 버전 통합: 4개 → 1개 (`10yp_2.py`)
- ✅ DB 통합: `my_scheduler.db` + `my_scheduler_old.db` → `scheduler.db`
- ✅ 공통 모듈 분리: `core/` 패키지로 추출
- ✅ 임시 파일 정리: probe/verify 스크립트 → `archive/` 이동

## 3. 코드 품질 이슈 (발견 순)

### 3.1 중복 코드
- `SolarTerms`(24절기 맵), `DBManager`, 월간 달력 렌더링, 엑셀 내보내기 로직이 4개 파일에 걸쳐 사실상 4벌 존재.

### 3.2 잠재 버그
1. **주간 키 불일치** (`10yp_2.py:738` vs `10YP.py:521`): `strftime('%Y-%U')`는 일요일 시작 주차 계산이 `%W`/ISO와 혼동되기 쉽고, `10YP.py`의 `W-YYYY-MM-DD`와 호환되지 않음 → 주간 메모 데이터 이월 시 유실.
2. **년도 미지정 음력 기념일 처리:** `10yp_2.py`의 `is_repeat` 로직상 year=0(매년 반복)인 기념일의 음력→양력 변환 결과를 매년 재계산해야 하나, DB에는 변환된 양력 월/일만 저장됨 → 매년 날짜가 어긋날 수 있음.
3. **`10YP.py:202` 부근 DailyEditor:** `font=("bold", 11)`처럼 튜플 첫 요소에 "bold"를 넣는 잘못된 폰트 지정이 다수 (`10YP.py:526`, `:460`, `:704` 등). tkinter에서는 `("맑은 고딕", 11, "bold")` 형태여야 함.
4. **`10YP.py:712` 정렬 후 `W-` 필터:** 주간 요약 행을 정렬 *이후*에 걸러내므로 동작은 하지만, DB 레벨에서 `WHERE date NOT LIKE 'W-%'`로 걸러내는 것이 효율적.
5. **`except:` 남용** (`10YP.py:153,161,651,813` 등): 디버깅을 방해하므로 구체적 예외 지정 필요.
6. **D-Day `tree_dday.item(...)`에서 id를 문자열로 읽음** (`10yp_2.py:1035` 등): `delete_task`/`delete_anniversary`에서 values[0]이 str인지 int인지 확인 필요 (Treeview는 str 반환 → `int()` 변환 누락 시 SQLite에서 매칭 실패 가능성 낮음(타입 어댑터)하나 명시적 변환 권장).

### 3.3 구조 개선 제안 (MVC 분리)
```
My_Scheduler/
├── main.py              # 진입점 (최신 버전 하나만)
├── core/
│   ├── solar_terms.py   # 24절기 (정적 메서드)
│   ├── lunar_utils.py   # 음력 변환/공휴일 헬퍼
│   └── db_manager.py    # 단일 스키마 DB 매니저
├── ui/
│   ├── monthly_view.py
│   ├── weekly_view.py
│   ├── report_view.py
│   └── editors.py       # 팝업 다이얼로그
└── data/
    └── scheduler.db     # DB 파일 단일화
```

## 4. 정리 작업 체크리스트

- [x] 1. **단일 버전 확정 (2026-09-04 완료):** 최신 기능을 갖춘 `10yp_2.py`를 베이스로 선정
- [x] 2. **구버전 아카이브 (2026-09-04 완료):** `my_scheduler.py`, `my_scheduler_old.py`, `my_scheduler_fixed.py`, `10YP.py`, `inspect_db.py` 및 DB 백업본(`my_scheduler_backup_20260904.db`, `my_scheduler_old_backup_20260904.db`)을 `archive/`로 이동
- [x] 3. **DB 통합 (2026-09-04 완료):** `migrate_db.py`로 구버전 DB 2개를 검사·이관한 결과,
  - 구버전 DB에는 기념일 테이블 자체가 없었고, 일정은 총 1건(`2026-09-24: aSDS`)뿐이었음
  - `categories`(4~5건)/`notes`(0건) 테이블은 최신 버전에 해당 기능이 없어 이관 생략 (원본 DB는 루트에 보존)
  - 최신 스키마(`schedules`, `anniversaries`, `tasks`, `ddays`)의 `scheduler.db` 생성 완료
- [x] 4. **공통 모듈 분리 (2026-09-04 완료):** `core/` 패키지 생성 — `core/solar_terms.py`(SolarTerms), `core/db_manager.py`(DBManager) 추출. `10yp_2.py`에서는 `from core.solar_terms import SolarTerms`, `from core.db_manager import DBManager`로 임포트. 검증: 24절기 계산(9/7=백로), DB roundtrip, 기본 기념일 11건 자동 생성, `10yp_2.py` 임포트 성공
- [x] 5. **주간 키 포맷 표준화 (2026-09-04 완료):** `10yp_2.py`의 `summary_key`를 `W-YYYY-%U` → `W-YYYY-MM-DD`(주 시작 일요일)로 변경. 기존 DB에 `W-` 키 데이터가 없어 마이그레이션 불필요. 구버전 `10YP.py`와 동일 포맷으로 통일됨
- [x] 6. **폰트 튜플 수정 (2026-09-04 확인):** `("bold", N)` 문제는 아카이브한 구버전에만 존재 — `10yp_2.py`에는 해당 없음 (추가 조치 불필요)
- [x] 7. **예외 처리 정리 (2026-09-04 완료):** `10yp_2.py`의 bare `except:` 7곳을 `sqlite3.Error`, `(ValueError, IndexError)`, `(KeyError, TypeError, ValueError, sqlite3.Error)` 등 구체적 예외로 교체. 잔존 0곳 확인 (`verify_cleanup.py`로 검증)
- [x] 8. **requirements.txt 작성 (2026-09-04 완료):** `requirements.txt` 생성 (ttkbootstrap, pandas, openpyxl, korean-lunar-calendar). 실제 설치 환경에서 4개 라이브러리 모두 임포트 성공 확인

### 진행 로그 (2026-09-04)
1. `archive/` 폴더 생성 후 구버전 소스 4개 + DB 백업본 2개 이동/복사 완료
2. `inspect_db.py`(→archive 이동)로 구버전 DB 스키마 확인: 예상과 달리 `journal`/`anniversaries` 컬럼·테이블 부재, `categories`/`notes` 테이블 존재
3. `migrate_db.py` 작성(스키마 자동 감지 방식) 및 실행 → `scheduler.db` 생성, 일정 1건 이관
4. `10yp_2.py` 및 `migrate_db.py` `py_compile` 검사 통과
5. `10yp_2.py` bare `except:` 7곳 구체적 예외로 교체, `requirements.txt` 생성
6. 최종 검증(`verify_cleanup.py`, →archive 이동): bare except 0곳 / 모듈 임포트 성공 / 의존성 4종 설치 확인 / `scheduler.db` 필수 4개 테이블 존재 확인
7. **주간 키 표준화:** `10yp_2.py:738` `W-%U` → `W-%Y-%m-%d` (678행 부근의 `W-` 판별 조건은 포맷 무관하여 수정 불필요)
8. **core 패키지 분리:** `core/__init__.py`, `core/solar_terms.py`, `core/db_manager.py` 생성, `10yp_2.py`에서 클래스 정의 제거 후 임포트로 전환 (약 220줄 중복 제거)
9. 통합 검증(`verify_core.py`, →archive 이동): 24절기/DB roundtrip/기본 기념일 11건/모듈 임포트/클래스 잔존 없음/주간 키 표준화 모두 통과

### ✅ 정리 완료 (2026-09-04)
체크리스트 전 항목(1~8번) 완료. 남은 것은 GUI 실행 테스트(`python 10yp_2.py`)뿐.

### ➕ 추가 기능 (2026-09-04 ~ 2026-09-05)
1. **테마 변경:** 기본 테마 `flatly` → `darkly` (어두운 테마 계열)
2. **월간/주간 셀 배경색을 전체목록(Tableview) 스타일로 변경:**
   - 하드코딩 RGB 대신 **테마 기본색(`self.style.colors`)** 사용 — 전체목록/설정 등과 동일한 테마 룩, 테마를 바꾸면 달력도 자동 연동
   - 셀 배경은 테마 `colors.bg`, 텍스트는 `secondary`/`primary`(토)·`danger`(일·공휴일), 주간 요약 셀도 `colors.bg` + `colors.light` 헤더 + `colors.fg` 텍스트로 통일
3. **공부 탭 신설 (사이드바 '📚 공부' 메뉴):**
   - DB에 `categories`, `study_notes`, `study_words`, `study_progress` 테이블 추가 (`core/db_manager.py`)
   - 기본 과목(영어/수학/기타) 자동 생성 + 과목 추가/삭제
   - **노트 정리:** 과목별 제목+내용 작성/저장/삭제/불러오기
   - **단어·용어 정리:** 과목별 용어/뜻 추가·삭제·새로고침
   - **매일 진도 체크:** 날짜별 과목/내용 기록, 더블클릭으로 완료 토글(✅/⬜)
   - 검증(`verify_study.py`): 기본 과목/노트/단어/진도/toggle/과목 삭제 모두 통과, `scheduler.db`에 공부 테이블 반영 완료
4. **뉴스/정보 탭 신설 (사이드바 '📰 뉴스 & 정보' 메뉴):**
   - `core/news_fetcher.py` 모듈 추가 (표준 라이브러리 urllib + xml.etree만 사용, 외부 의존성 없음)
   - 카테고리별 RSS 총 15개 소스 (전부 실제 네트워크 검증, 15/15 OK):
     * 일반: 연합뉴스 종합/사회, 한겨레, 경향신문
     * 과학: 연합뉴스(IT과학), 네이처, NewScientist, ScienceDaily, PhysOrg
     * IT: 테크크런치, TheVerge, Gizmodo
     * 건축: ArchDaily, Dezeen
     * 재테크: 연합뉴스(경제)
   - **카테고리 필터 콤보:** 전체/일반/과학/IT/건축/재테크 선택 시 해당 소스만 수집
   - **브라우저 열기:** 기사 아래 파란색 링크 클릭 시 `webbrowser.open`으로 기사 원문 열기 (링크에 손모양 커서 표시)
   - **실시간 수집 (🔄 새로고침 버튼):** 스레드 비동기 + `ThreadPoolExecutor` 병렬 수집 → UI 블로킹 방지
   - 수집 결과: 출처별 1~15건씩 날짜/제목/요약/링크 표시
   - 검증: 15/15 소스 정상 수집 확인 (RSS/Atom/RDF 네임스페이스 + BOM 자동 처리)
5. **단어장 DB 신설 (사이드바 '📕 단어장 (Word DB)' 메뉴):**
   - `wordbook` 테이블 추가 (`core/db_manager.py`), 저장 형식: **순위 | 단어/숙어 | 발음(영어) | 발음(한국어) | 품사 | 뜻(한국어) | 예제 | 예제뜻**
   - `core/word_data.py` 기본 데이터 시드 (언어별 자동 삽입, 없으면 `LANG_WORDS`에 추가):
     * **영어 기본 동사 300개** (be ~ roll, 발음기호+한국어 독음+예문 포함)
     * 일본어 기본 단어 20개 / 중국어 기본 단어 20개 (시작용)
   - 기능: 언어 선택 콤보(영어/일본어/중국어 + 새 언어 추가/삭제), 검색(단어·뜻·발음), 표 클릭 시 입력창 불러오기, 추가/수정/삭제, '입력창 비우기'
   - 언어 삭제 시 해당 언어의 모든 단어 일괄 삭제, 전체/언어별 단어 수 표시
   - 검증(`verify_worddb.py`): 영어 300/일본어 20/중국어 20 자동 삽입, 검색·추가·수정·삭제 roundtrip 모두 통과 (총 340개)
6. **생활용어 DB 신설 (사이드바 '💬 생활용어 (Phrases)' 메뉴):**
   - `phrases` 테이블 추가 (`core/db_manager.py`), 저장 형식: **순위 | 문장 | 발음(한국어) | 뜻(한국어)**
   - `core/phrase_data.py` 기본 문장 시드 (언어별 자동 삽입, 없으면 `LANG_PHRASES`에 추가):
     * **영어 기본 생활용어 300개** (인사/예의 · 소개/대화 · 시간/날짜 · 길찾기/교통 · 식당/음식 · 호텔 · 쇼핑 · 긴급/건강 · 일상/집 · 일/공부 · 날씨/기분 · 전화/인터넷 · 유용한 표현 · 작별)
     * 일본어 기본 문장 20개 / 중국어 기본 문장 20개
   - 기능: 언어 선택 콤보(영어/일본어/중국어 + 새 언어 추가/삭제), 검색(문장·뜻·발음), 표 클릭 시 입력창 불러오기, 추가/수정/삭제, '입력창 비우기'
   - 언어 삭제 시 해당 언어의 모든 문장 일괄 삭제, 전체/언어별 문장 수 표시
   - 검증(`verify_phrase.py`): 영어 300/일본어 20/중국어 20 자동 삽입, 형식·순위(1~300) 확인, 검색·추가·수정·삭제 roundtrip 모두 통과 (총 340개)
7. **Year Progress 일자형 변경 (2026-09-06):**
   - 반원형 `Meter`(160px, 텍스트 중첩) → **가로 일자형 `ttk.Progressbar`** + 별도 라벨로 변경해 "N일 지남 / M일 남음 (N%)"이 겹치지 않고 잘 보이게 수정
   - `update_year_progress()`가 `progress_bar`(값)와 `year_progress_label`(텍스트)를 각각 갱신, `Meter` import 제거
8. **뉴스 저장/삭제 기능 (2026-09-06):**
   - `saved_news` 테이블 추가: 제목/링크(UNIQUE)/설명/발행일/출처/분류/저장시각 (`core/db_manager.py` CRUD)
   - 뉴스 탭을 **2단 구조**로 재설계:
     * 상단 **📥 실시간 수집 뉴스** (Treeview: ★표시/출처/날짜/제목) — 행 선택 후 **⭐ 선택 저장** / **🔗 링크 열기**, 더블클릭 브라우저 열기
     * 하단 **⭐ 저장한 뉴스** (Treeview: 저장일시/출처/분류/제목) — 행 선택 후 **🗑 선택 삭제**, 저장 뉴스 검색, 더블클릭 브라우저 열기
   - 저장 시 중복(같은 링크) 방지, 저장된 기사는 수집 목록에서 ★ 표시, 삭제 시 ★ 해제
   - 검증(`verify_news_db.py`): 신규 저장/중복 거부/조회/검색/삭제/카운트 전부 통과
9. **뉴스 분야별 체계화 (2026-09-06):**
   - 소스 21개 · 분야 **9개**로 재편 (전부 실제 네트워크 검증 21/21 OK):
     | 분야 | 소스 |
     |---|---|
     | 일반 | 연합뉴스(종합), 한겨레 |
     | **정치** | 연합뉴스(정치), 한겨레(정치) |
     | **사회** | 연합뉴스(사회), 경향신문 |
     | **경제** | 연합뉴스(경제), 한겨레(경제) |
     | **부동산** | Google 부동산(토픽) |
     | **재테크** | Google 재테크, Google 주식(토픽) |
     | 과학 | 연합뉴스(IT과학), 네이처, NewScientist, ScienceDaily, PhysOrg |
     | IT | 테크크런치, TheVerge, Gizmodo |
     | 건축 | ArchDaily, Dezeen |
   - 카테고리 콤보가 자동으로 9개 분야 표시, 저장 뉴스의 분류 컬럼에도 반영
   - 국내 매체별 부동산 RSS는 전부 살아있지 않아 **Google News 토픽 RSS**로 대체 (부동산/재테크/주식 검색피드, 안정 동작 확인)
10. **뉴스 검색 수집 & 분류 저장 수정 (2026-09-06):**
    - **저장 분류 버그 수정**: 저장 시 카테고리 콤보값(보통 '전체')을 쓰던 것을 → **각 기사의 실제 소스 분류**(`get_source_category`)로 저장하도록 변경 (정치/사회/경제/부동산/재테크/과학/IT/건축/일반/검색)
    - **검색어 수집 추가**: 헤더에 `검색` 입력창 + `검색`/`초기화` 버튼 추가. 검색어 입력 시 Google News 검색 RSS(`build_search_sources`)로 해당 주제 기사를 실시간 수집 (Enter 키 지원)
    - 검색 결과는 출처명 `검색: <키워드>`, 분류 `검색`으로 표시·저장
    - 검증(`verify_search.py`): 'AI 반도체' 검색 5건 수집(0.8s), 소스→분류 매핑, 분류 저장 roundtrip 모두 통과

11. **증권 (Market) 탭 신설 (2026-09-06):**
    - `core/market_fetcher.py`: 금융원 OpenAPI 호출 클래스 (주식/상품/지수/회사 4종 API, Mock 폴백)
    - `saved_markets` 테이블 추가 (`core/db_manager.py`), 저장 형식: **symbol | name | price | category | source_api | saved_at**
    - UI: **검색 입력란 + 🔎검색/⭐저장/🗑삭제 버튼** → 결과 Treeview → **종목 클릭 시 하단 정보 라벨 + 차트**
    - Mock 데이터 5건 (삼성전자/SK하이닉스/네이버/기아/현대차) — API 키 미입력 시 자동 표시
    - API 키 입력 시 실시간 데이터로 자동 전환
12. **API 키 JSON 저장 + 설정 탭 관리 (2026-09-06):**
    - `config.json`: API 키 4종 + 테마 설정 저장
    - `core/config_manager.py`: 설정 로드/저장 모듈
    - **설정 탭**에 "🔑 금융원 OpenAPI 인증키 관리" 섹션 추가:
      * 주식시세정보/일반상품시세정보/지수시세정보/금융회사기본정보 4개 입력란
      * 💾 API 키 저장 / 🔄 새로고침 버튼
      * 키는 `config.json`에 안전하게 저장, 앱 시작 시 자동 로드
13. **전체 정리 + 문서 업데이트 (2026-09-06):**
    - 버전 통합 완료 (4개 → 1개 `10yp_2.py`)
    - DB 통합 완료 (`scheduler.db` 단일화)
    - 공통 모듈 `core/` 패키지로 분리 (8개 모듈)
    - 임시 파일 `archive/` 이동, 프로젝트 구조 정리
    - 문서 업데이트

## 5. 설정 파일 (config.json)
```json
{
  "api_keys": {
    "fss_stock": "",
    "fss_product": "",
    "fss_index": "",
    "fss_company": ""
  },
  "theme": "darkly",
  "version": "2026-09-06"
}
```

## 6. 의존성 (requirements.txt)
```
ttkbootstrap
pandas
openpyxl
korean-lunar-calendar
```

## 7. 실행 방법
```bash
python 10yp_2.py
```

## 8. 다음 단계 (선택)
- 금융원 API 키 입력 → 실시간 증권 데이터 활성화
- 설정 탭에서 API 키 저장 → 재시작 시 자동 로드
- 뉴스 소스 추가/편집은 `core/news_fetcher.py`의 `NEWS_SOURCES` 수정
- 증권 Mock 데이터 확장은 `core/market_fetcher.py`의 `_mock()` 수정
