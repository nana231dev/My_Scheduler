# 🔍 My_Scheduler 전수 검사 및 개선 계획 (2026-09-13)

> **검사 방법:** 23개 Python 파일 컴파일 + 19개 core 모듈 임포트 + unittest 21건 실행 +
> 임시 DB로 신규 DB 기능(복습/투자일기/뉴스읽음) 런타임 실측 +
> `10yp_2.py` 3,042줄 전체 정독 + DB 스키마/컬럼 실측 + 데이터 카운트 실측
> **검증 스크립트:** `Temp/full_inspection.py`, `Temp/inspect2.py`, `Temp/inspect3.py` (결과: `Temp/*_result.txt`)
>
> **✅ 2026-09-14 후속 문서:** `2026-09-14_종합_재검토_및_개선계획.md` — 전체 재검토·문제점 진단·전문가 제안·git 상태(원격 저장소 미생성) 최신화
>
> **✅ 2026-09-13 조치 완료 (후속 세션):** 아래 A표 6건 전부 해결 + 불필요 파일 정리 완료.
> 최종 회귀 검증: **컴파일 22/22 OK, unittest 21/21 OK** (`Temp/verify_fix.py` → `Temp/verify_fix_result.txt`)
>
> **✅ 2026-09-13 다음 단계 구현 완료:** 뉴스 읽음 표시 UI · 즐겨찾기 목록 뷰 · 복습 시스템 UI · 투자 일기 UI · 데이터 백업/복원.
> 최종 회귀 검증: **컴파일 22/22 OK, unittest 24/24 OK** (신규 테스트 3건 포함) + DB 스모크 테스트 통과 (`Temp/verify_next.py` → `Temp/verify_next_result.txt`)
>
> **✅ 2026-09-13 중기 3종 구현 완료:** 시작 리마인더 팝업 · 증권 API 실측/정규화(엔드포인트 폐기 확인) · i18n/exceptions 실적용 —
> **컴파일 22/22 OK, unittest 27/27 OK** (`Temp/verify_mid.py` → `Temp/verify_mid_result.txt`)

---

## 📌 요약 (한눈에 보기)

| 구분 | 결과 |
|---|---|
| 컴파일 | ✅ **23개 파일 OK** / ❌ 1개 FAIL (`core/db_manager_backup.py` 인코딩 손상) |
| 모듈 임포트 | ✅ 19/19 OK (단, `core/db_manager_new.py`는 임포트 시 debug print 출력하는 미완성 스텁) |
| 테스트 | ✅ **unittest 21/21 통과** (pytest는 미설치) |
| 신규 DB 기능 | ✅ 복습 시스템·투자 일기·뉴스 읽음 DB 메서드 **실동작 확인** (임시 DB) |
| 앱 실행 | ✅ 23파일 컴파일 OK로 정상 실행 가능 (실행 시 `investment_journal` 테이블 자동 생성됨) |
| 발견된 문제 | ✅ 즉시 조치 6건 전부 해결 완료 (2026-09-13 세션) · ⚠️ DB↔UI 격차 4종(복습/투자일기/뉴스읽음/즐겨찾기목록)만 진행 예정 |
| 버전 관리 | 🔴 **git 미설치** — 이력 없음 (즉시 설치 권장) |

---

## 📊 실측 데이터 카운트 (2026-09-13 기준)

| 모듈/데이터 | 실측 | 이전 문서 기준 |
|---|---|---|
| `core/formulas.py` | **160개** (levels: 초등/중등/고등/대학) | 160 ✅ |
| `core/glossary.py` | **100개** | 100+ ✅ |
| `core/idiom_data.py` | **67개** | 67 ✅ |
| `core/hanja_data.py` | **380개** | 380 ✅ |
| `core/science_data.py` | **86개** | 80+ ✅ |
| `core/history_data.py` | **53개** | 56+ ⚠️ (문서와 불일치) |
| `core/quote_data.py` + `data/quotes_data.json` | **345개** | 345 ✅ |
| `core/word_data.py` (단어 시드) | 영어동사 300 + 일본어 20 + 중국어 20 + 언어필터 3 = **343** | 340 ⚠️ |
| `core/phrase_data.py` (생활용어 시드) | 영어 300 + 일본어 20 + 중국어 20 + 언어필터 3 = **343** | 340 ⚠️ |
| `core/news_fetcher.py` | 등록 **42건 / 유니크 41개** (중복 1건 ⚠️) / 분야 **16개** | 42소스 ⚠️ |
| `tests/test_core.py` | **21건 통과** | 21 ✅ |

---

## 📈 오늘(2026-09-13)까지의 진행상황

### 어제까지 완료 (2026-09-12 기준)
- 단일 파일 앱(`10yp_2.py`) + `core/` 19모듈 구조, 사이드바 9탭 구조
- 공부 탭 12개 서브탭 데이터 연동, 공식집/용어집 UI, 뉴스 키워드 분석, 가계부 탭
- 재구축 모듈: logger / exceptions / i18n / formulas / glossary / news_summarizer / quote_data

### 오늘 완료 (2026-09-13, 이전 세션)
- **Phase 4~6 런타임 버그 수정:** `_clear_tree` 추가, 가계부 SQL 인코딩 복구, 일간 그림 저장(INSERT OR REPLACE), `schedule_images` 컬럼 보장, 증권 Mock 폴백, `Path` import, `update_saved_stock` 추가
- **데이터 확장:** 명언 345개, 한자 380개, 사자성어 67개, 뉴스 42소스·16분야
- **DB 인코딩 복구** (`core/db_manager.py` — 런타임 문자열 `'수입'`/`'지출'` 정상화)
- **명언 즐겨찾기 DB+UI 연동** ⭐: `favorite_quotes` 테이블 + `10yp_2.py` 토글 버튼 실연동 확인 ✅
- **오늘의 명언/고사성어/한자** 월간 뷰 상단 표시 + 의미 보기 팝업

### 오늘 신규 추가 확인 (DB 계층, 이번 세션 검증 완료)
- 복습 시스템: `get_words_due_for_review()` / `mark_word_reviewed()` — wordbook에 `ease`, `reviewed_at` 컬럼 ✅
- 투자 일기: `add_investment()` / `get_investments()` / `get_investment_summary()` — `investment_journal` 테이블 ✅
- 뉴스 읽음 표시: `mark_news_read()` / `reset_news_read()` — saved_news에 `read_flag` 컬럼 ✅

> ⚠️ **중요:** 위 3종은 **DB 메서드만 추가된 상태이고 UI 연동은 0건** (아래 격차표 참조)

---

## 🔴 발견된 문제점 (전수 검사 결과)

### A. 즉시 조치 필요 (버그/보안)

| # | 위치 | 문제 | 처리 결과 (2026-09-13 완료) |
|---|---|---|---|
| 1 | `core/market_fetcher.py` | 🔐 소스 코드에 실제 금융원 API 키 하드코딩 | ✅ 하드코딩 제거 → `config.json` 경유 통일 (`__main__`도 `get_api_key()` 사용, 실동작 확인) |
| 2 | `core/news_fetcher.py:31,33` | "Google 부동산" 동일 URL 2건 중복 | ✅ 중복 1건 삭제 → **41소스 · 중복 0건** |
| 3 | `core/db_manager_backup.py` | 문법 오류 (라인 211 인코딩 손상, 컴파일 실패) | ✅ `Temp/` 이동 (core에서 제외) |
| 4 | `core/db_manager_new.py` | 18줄 미완성 스텁 (임포트 시 print) | ✅ `Temp/` 이동 (core에서 제외) |
| 5 | `10yp_2.py:1831` | 가계부 월요약 `except: return` bare except | ✅ `except (ValueError, AttributeError): return` |
| 6 | `core/db_manager.py:1040` | `search_subject_data` return 뒤 도달 불가 `commit()` | ✅ 삭제 |

### B. 기능 격차 — DB는 완성, UI 미연동 (0건)

| 기능 | DB 메서드 | UI(`10yp_2.py`) 연동 | 필요한 UI |
|---|---|---|---|
| 복습 시스템 | ✅ `get_words_due_for_review`, `mark_word_reviewed` | ❌ 0건 | "오늘 복습할 단어" 패널 + 복습 성공/실패 버튼 |
| 투자 일기 | ✅ `add_investment`, `get_investments`, `get_investment_summary` | ❌ 0건 | 증권 탭에 투자일기 서브뷰 (입력 폼 + 월별 손익 라벨 + 목록) |
| 뉴스 읽음 표시 | ✅ `mark_news_read`, `reset_news_read` | ❌ 0건 | 저장 뉴스 목록에 읽음 컬럼·색상 + 열기 시 자동 표시 |
| 즐겨찾기 목록 뷰 | ✅ `get_favorite_quotes`, `count_favorite_quotes` | ❌ 0건 (⭐ 토글만 연동) | 명언 탭에 "⭐ 즐겨찾기 목록" 보기 버튼 |

### C. 재구축했으나 앱에 미적용인 모듈 (인프라 격리)

| 모듈 | 상태 | 적용 방안 |
|---|---|---|
| `core/i18n.py` (ko/en/ja 40키) | 앱 임포트 0건 | 상단 버튼/헤더 문자열부터 `t()` 적용 |
| `core/exceptions.py` (safe_call 등) | 앱 임포트 0건 | 뉴스/증권 네트워크 호출에 `safe_call`/`retry_on_error` 적용 |
| `core/logger.py` | 앱 시작 연동 ✅, 그러나 `market_fetcher.py`는 여전히 `print()` | `get_logger()`로 전환 |

### D. 기술 부채 (계획 조치)

| # | 항목 | 상세 |
|---|---|---|
| 1 | 🔴 **버전 관리 없음** | git 미설치. 파일 이력/백업/병합 불가 → **설치 + 초기 커밋 최우선** |
| 2 | ✅ API 키 정리 완료 | 소스 내 하드코딩 키 제거(→`config.json` 단일 경유). git 도입 시 `.gitignore`에 `config.json`, `*.db`, `data/images/`, `Temp/`, `__pycache__/` 등록 필요 |
| 3 | `10yp_2.py` 3,042줄 단일 파일 | 계속 성장 중 → `ui/` 분리 재검토 (안정화 후) |
| 4 | `core/db_manager.py` 주석 다수 인코딩 손상 | 런타임 동작은 정상이나 가독성 저조 (2행 독스트링, 156행, 849행 등) → 주석 재작성 |
| 5 | pytest 미설치 | `pip install pytest` + `requirements-dev.txt` 생성 |
| 6 | `tests/test_core.py:163` | `import logging`이 하단 배치 → 상단으로 이동 |
| 7 | ✅ 루트 정리 완료 | 깨진/스텁 py 2건 → `Temp/` 이동. 구 DB 2건·`Old/` 폴더·`data/images`·Temp 이미지는 **사용자가 직접 삭제** (2026-09-13). `__pycache__`는 실행 시 자동 재생성되는 캐시 |
| 8 | `2026-09-12_전체_진행상황_정리.md` 명언 즐겨찾기 ❌ 표기 | 실제로는 DB+UI 연동 완료 → 문서 갱신 필요 |
| 9 | `history_data` 실측 53 vs 문서 56+ | 문서 또는 데이터 확인 필요 |

---

## ✅ 정상 확인된 사항 (문제없음)

- `10yp_2.py` 전체 구조: DailyEditor / SchedulerApp / switch_view 9케이스 / 뷰 메서드 패턴 일관성 유지
- 가계부: `setup_ledger_view` + `transactions` CRUD + `get_month_summary`/`get_category_summary` (복구 상태 확인)
- 기념일 계산: 양력/음력/윤달/설날·추석 연휴/대체공휴일 로직 정상
- 설정 탭: 테마 변경 + API 키 4종 저장/새로고침 (`config.json` 경유) 정상
- 임시 DB 실측: 복습 20건 선별 → `mark_word_reviewed` OK / 투자일기 입력→요약→삭제 OK / 뉴스 읽음 OK
- `config_manager.py`, `quote_data.py` 등 신규 모듈 코드 품질 양호 (타입 힌트, 독스트링 완비)

---

## 🗓 앞으로 해야 할 일

### 🚀 단기 (이번 주 — 청소 + UI 연동 + 안전망)

| 순서 | 작업 | 상태 (2026-09-13) |
|---|---|---|
| 1 | git 설치 + 초기 커밋 | ⚠️ git 미설치 확인 — `.gitignore`·`requirements-dev.txt` 생성 완료, 설치 후 `git init → add → commit`만 실행하면 됨 |
| 2 | 코드 청소 6건 | ✅ 완료 (위 A표 참조) |
| 3 | 뉴스 읽음 표시 UI | ✅ 완료 — "읽음" 컬럼·✅ 표시·회색 처리·링크 열기 시 자동 읽음·"✅ 읽음"/"↩ 읽음 초기화" 버튼, (안읽음 N) 카운트 |
| 4 | 즐겨찾기 목록 뷰 | ✅ 완료 — 월간 탭 "⭐ 목록" 버튼 + 팝업(유형/내용/출처/추가일시 조회 + 선택 삭제) |
| 5 | 복습 시스템 UI | ✅ 완료 — 단어장 "🔁 오늘 복습" 버튼 + 카드형 팝업(뜻 확인 → 성공/실패 → ease 갱신), DB SELECT에 발음/예문 필드 추가 |
| 6 | 투자 일기 UI | ✅ 완료 — 증권 탭 "📔 투자일기" 팝업(입력 폼·월 필터·실현손익 요약·목록·삭제) |
| 7 | 데이터 백업/복원 | ✅ 완료 — 설정 탭 "💾 지금 백업"/"↩ 백업 파일에서 복원" (backup/ 타임스탬프, DB 연결 종료 후 복원) |
| 8 | 루트 정리 | ✅ 완료 — 구 DB·깨진/스텁 py 정리, `test_core.py` 하단 `import logging` → 상단 이동 |

### 📌 중기 (2~4주)

| 순서 | 작업 | 비고 |
|---|---|---|
| 9 | ✅ 알림/리마인더 완료 — 시작 시 오늘 일정/D-Day(30일)/복습 대상/월 지출 요약 팝업 + "시작 시 표시" 토글(config.json `show_reminder`) | 2026-09-13 |
| 10 | ✅ 증권 실데이터 진단/파싱 완료 — **실측: 기존 엔드포인트 폐기**(HTML 오류 페이지), `api.fss.or.kr` 부존재(DNS 실패). HTML 감지+명확한 안내, `normalize_fss_response` 정규화 준비(테스트 3건), print→logger 전환 | 2026-09-13 |
| 11 | ✅ i18n/exceptions 실적용 완료 — 리마인더 팝업 `i18n_t()` 8키 적용, 뉴스/증권 네트워크 호출 `handle_error` 3곳 | 2026-09-13 |
| 12 | 테스트 확장 21 → **27건** 완료 (뉴스 읽음·복습 필드·투자일기·응답 정규화 6건 추가) | ✅ 부분 완료 — 잔여: 가계부/증권 저장목록 테스트 |
| 13 | `core/db_manager.py` 주석 인코딩 복구 (전면 재작성) | 가독성/유지보수 |

### 🔵 장기 (1~2개월)

| 순서 | 작업 | 비고 |
|---|---|---|
| 14 | AI 비서 (Gemini) — `core/ai_helper.py` + 단어 예문/번역 | config에 키 이미 관리 중 |
| 15 | 통합 검색 (Ctrl+F) — `core/search.py` + 사이드바 검색창 | |
| 16 | ICS 캘린더 동기화 — `core/cloud_sync.py` | 재구축 잔여 목록 |
| 17 | 문서 요약기 — `core/doc_summarizer.py` | 외부 의존성 별도 설치 |
| 18 | **`ui/` 패키지 분리** 재검토 (10yp_2.py 3,000줄 → 뷰 파일 단위) | 안정화 조건부 |
| 19 | exe 빌드 — `pyinstaller --onefile --windowed 10yp_2.py` | |

---

## 💡 이번 세션 권장 다음 액션 (Top 3)

1. **git 설치 + 초기 커밋** — `.gitignore`·`requirements-dev.txt` 준비 완료. git 설치 후 `git init && git add . && git commit -m "init"`만 실행하면 됨
2. ~~단기 로드맵 전체~~ ✅ 완료 (청소 6건·UI 4종·백업·리마인더·증권 파싱·i18n/exceptions)
3. **잔여 항목** — 공공데이터포털 연동은 확인 완료(`GetStockSecuritiesInfoService_V2/getStockPriceInfo_V2` 실응답), 남은 문제: 투자일기 저장 시 금액 쉼표 ValueError 처리, 지수/일반상품/금융회사 서비스 식별자·권한 재조사, 키 등록 상태 확인. 가계부/증권 저장목록 테스트 추가, `db_manager.py` 주석 인코딩 복구

---

## 📎 부록 — 검증 결과 원문 위치

- 컴파일/임포트/파일 규모: `Temp/full_inspection.py` (10yp_2.py 3,042줄 / core+tests 합계 8,109줄)
- DB 런타임 실측: `Temp/inspect2.py` → `Temp/inspect2_result.txt` (16테이블, ease/reviewed_at/read_flag/investment_journal 존재, 신규 메서드 동작 OK)
- 데이터 카운트: `Temp/inspect3.py` → `Temp/inspect3_result.txt` (343/343 시드, 뉴스 42/41 유니크·중복 1건)
- 테스트 실행: `python -m unittest tests.test_core` → **21/21 OK**
