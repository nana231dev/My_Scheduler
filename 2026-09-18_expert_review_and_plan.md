# My_Scheduler 전문가 종합 진단 및 개선 계획 (2026-09-18 기준)

> 작성일: **2026-09-18** / 대상: `f:\Git_home\My_Scheduler` / 방식: **코드·DB·런타임 실측 기반**

---

## 0. 진단 방법과 근거 (재현 가능)

"느낌"이 아니라 실측으로 판정했다. 모든 수치는 아래 산출물로 재현 가능하다.

| 산출물 | 측정 내용 |
|---|---|
| `Temp/smoke_views.json` | 앱 초기화 + 9개 뷰 전환 스모크 결과 |
| `Temp/smoke_views_out.txt` | 증권 API 라이브 호출 로그 (URL·totalCount) |
| `Temp/ut_now.txt` | 유닛테스트 결과 (34 tests) |
| `Temp/state_probe.json` | DB 17테이블 행수 / 설정 / 파일 존재 |
| `Temp/diag_final.json` | 소스 손상(강신호) 라인수 / DB 텍스트 오염 |

측정 스크립트: `Temp/smoke_views.py`, `Temp/state_probe.py`, `Temp/diag_final.py`
(모두 `Temp/`는 `.gitignore` 대상 → 저장소 오염 없음)

---

## 1. 오늘(2026-09-18) 전체 스냅

### 1-1. 실행 상태

| 항목 | 실측 결과 | 판정 |
|---|---|---|
| 앱 초기화 (`SchedulerApp()`) | `OK` | ✅ |
| 뷰 전환 9종 (monthly/weekly/daily/report/study/news/market/ledger/settings) | **9/9 예외 없음** | ✅ |
| 유닛테스트 | `Ran 34 tests — OK` | ✅ |
| `python -m py_compile 10yp_2.py` | 통과 | ✅ |
| 증권 API 라이브 | `삼성` → **totalCount=546** (`beginBasDt=20260819~endBasDt=20260918`) | ✅ |
| `ui/market_widget.py` | **파일 없음** → `market_widget_none=true` | ❌ |
| Git | `M 10yp_2.py` (미커밋 1건) | ⚠️ |

### 1-2. 데이터 상태 (DB 17테이블)

| 테이블 | 행수 | 해석 |
|---|---|---|
| schedules | 5 | 일정 사용 시작 단계 |
| transactions | 3 | 가계부 사용 시작 단계 |
| investment_journal | 1 | 투자일기 사용 시작 단계 |
| saved_markets | 4 | 증권 저장목록 |
| saved_news | 4 | 스 저장 |
| wordbook / phrases | 340 / 340 | 시드 데이터 정상 |
| study_words | 89 | 사용 중 |
| study_notes | 12 | 사용 중 |
| **study_progress / study_subject_data** | **0 / 0** | **도달 불가 또는 미사용** |
| **favorite_quotes / tasks** | **0 / 0** | **미사용(사실상 죽은 기능)** |
| categories / anniversaries | 16 / 11 | 정상, **오염 0건** |
| ddays | 2 | 사용 중 |

> **중요(Positive)**: 기존 사용자 데이터는 **손상 0건**. 문제는 "앞으로 생성/내보내는 것"에만 발생한다.

### 1-3. 설정 상태

| 키 | 상태 |
|---|---|
| `api_keys.fss_stock` | `SET(len=64)` ✅ 실동작 |
| `api_keys.fss_product / fss_index / fss_company` | `EMPTY` (해당 서비스 **data.go.kr에서 서비스 종료** → 재신청 필요) |
| `theme` | `darkly` |
| `show_reminder` | `true` |

---

## 2. 분야별 전문가 진단

### 2-A. 일정관리 도메인 (일정관리/생산성 앱 전문가)

**현황**
- 뷰 4종: 월간 달력 / 주간 플래너 / 일간 일지 / 전체 목록 (`switch_view`)
- 모델: `schedules(date PK, schedule, journal, schedule_images)` — 음력·절기·기념일 연동
- 부가: 시작 리마인더(오늘 일정 + D-30 + 복습대상 + 월 지출), 사이드바 요약, 이미지 첨부

**문제 (전문가 판정)**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| S-1 | **엑셀 내보내기-가져오기 왕복 불가** | 내보내기 컬럼이 `["?좎","?젙","鍮꾧","항목吏"]`(`db_manager.py:438`)인데, 가져오기 검증은 `['날짜','일정','비고']`(`10yp_2.py:1231`) → 자기가 내보낸 파일을 거부 | **P0** |
| S-2 | 가져오기 실행해도 **전 행 무음 스킵** | `import_schedules_from_df`가 `row['?좎']` 조회(`db_manager.py:460,464`) → 검증을 통과한 정상 파일도 `KeyError` → `log.warning`만 남고 성공 토스트 표시 | **P0** |
| S-3 | 셀 헤더가 사용자에게 깨져 보임 | `df.to_excel` 이 `?좎쫨` 문자열을 그대로 기록(`10yp_2.py:1222`) | **P0** |
| S-4 | `set_schedule`/`get_schedule` **이중 정의** — 앞 정의가 죽은 코드 | 앞: `269`/`257`행(3-arg), 뒤: `395`/`409`행(images 포함) → 뒤가 덮음 | P1 |
| S-5 | 동일 DDL 중복 기재 (`investment_journal` 이 `create_tables`와 `ensure_schedule_tables` 양쪽에) | `db_manager.py:199` vs `333` | P2 |
| S-6 | 일정 데이터가 5건뿐 → **검증 시나리오 부족** | DB 실측 | P2 |

**해야 할 일**
1. `get_all_schedules_with_images_df` 컬럼명을 `["날짜","일정","저널","이미지"]`로 **정정**하고, `import_schedules_from_df`의 키 조회를 **같은 이름으로 동시 변경** (한쪽만 고치면 즉시 깨짐 — 반드시 동시 수정)
2. 가져오기 검증 목록을 **실제 스키마와 일치**시키고, 실패 행이 있으면 **N건 스킵 결과를 사용자에게 보고**
3. 죽은 정의(256~286행) 삭제 → 단일 정의 유지
4. 엑셀 왕복 테스트 1건 추가: `export → import` 후 일정 수 동일 검증

---

### 2-B. 노트 및 학습 도메인 (학습 데이터 아키텍트)

**현황**
- 공부 탭 **13개 서브**: 단어장, 생활용어, 공식집, 용어집, 사자성어, 한자, 과학, IT, 화학, 물리, 지구과학, 사회탐구, 역사
- 시드: 단어장 340 / 생활용어 340 / 공식 160 / 용어 100+ / 한자 380 / 명언 345
- DB: `study_notes 12`, `study_words 89`

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| N-1 | **`study_progress` 0행 = "매일 진도 체크" 기능이 실제로 안 쓰이거나 도달 불가** | DB 실측 0 | **P1** |
| N-2 | **`study_subject_data` 0행** — 과목별 메모 저장 경로 미사용 | DB 실측 0 | P1 |
| N-3 | 시드 데이터가 코드에 하드코딩(단어 340줄 규모) → **번역/수정 시 코드 배포 필요** | `core/word_data.py`, `phrase_data.py` | P2 |
| N-4 | 복습 알고리즘(에빙하우스) 값이 리마인더에만 쓰이고, 진도 테이블과 연결 미확인 | `get_words_due_for_review` 사용처 1곳 | P2 |

**해야 할 일**
1. `study_progress` / `study_subject_data`의 **호출 경로 존재 여부를 코드로 확인** → 없으면 UI 노출, 있으면 기본값/조건 수정
2. 진도율 위젯(과목별 완료/전체)을 공부 탭 상단에 배치 — 데이터가 이미 있으므로 **표시만 하면 즉시 가치 발생**
3. 시드 데이터는 유지하되 `data/*.json` 이관은 **별도 트랙**으로 분리(현재 상태로도 동작)

---

### 2-C. 증권 도메인 (금융 데이터 엔지니어)

**현황 (여기가 최대 손상 지점)**
- 라이브 API **정상**: `GetStockSecuritiesInfoService_V2/getStockPriceInfo_V2`, `삼성`→546건, 30일 창(`20260819~20260918`)
- 저장목록 4건, 투자일기 1건

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| M-1 | **`ui/market_widget.py` 부재** → `setup_market_view`가 `except`로 낙하해 "⚠️ 증권 화면 위젯 로드 실패"만 표시 | `ENOENT` + `market_widget_none=true` | **P0** |
| M-2 | **`self._market_tree`를 만드는 코드가 어디에도 없음**(전 54개 참조가 읽기/사용) → 검색해도 **표에 아무것도 안 나옴** | `_market_tree` 사용처 `1631,1741,1776,1780,...` / 생성 0 | **P0** |
| M-3 | `_save_selected_market` / `_edit_selected_market` / `_delete_selected_market` / `_on_market_select` 가 `self._market_tree`를 **직접 참조** → 클릭 시 `AttributeError` | `1776, 1795, 1837, 1849, 1853` | **P0** |
| M-4 | `_search_market` 이 **UI 스레드에서 동기 HTTP 호출** → 검색 시 화면 정지 | `1725: mfk.search_stocks(keyword)` (스레드 없음) | **P1** |
| M-5 | API 실패 시 **Mock(가짜) 데이터를 조용히 표시** → 시세로 오인 위험 | `1733~1739` "실데이터 조회 실패 -> Mock 표시" | **P1** |
| M-6 | 구현이 **3갈래로 중복**: (a)비동기 `_fetch_and_render_market`+`_render_market_ui`, (b)동기 `_search_market`, (c)삭제된 `MarketWidget` | `1615~1641` vs `1706~1772` | P1 |
| M-7 | 지수/상품/기업 API는 **서비스 종료**로 사용 불가 | 이전 진단 14조합 전부 `NO_OPENAPI_SERVICE_ERROR` | P2(외부) |
| M-8 | 종목명 부분검색은 정상(494→546건) — **API 계층은 신뢰 가능** | 라이브 로그 | ✅ |

**해야 할 일 (순서 엄수)**
1. **단일 구현으로 통합**: `_search_market`을 **유일한 진입점**으로 삼고, Treeview를 `setup_market_view` 안에서 직접 생성(컬럼: 종목코드/종목명/종가/전일대비/등락률/거래량/시장)
2. 검색을 **백그라운드 스레드 + `after(0, ...)` UI 갱신**으로 변경 (M-4)
3. Mock 폴백은 **기본 비활성 + 상태 라벨에 명시 경고**(빨강) (M-5)
4. 숫자 포맷 통일 헬퍼(`_fmt_price`, `_fmt_change`, `_fmt_pct`, `_fmt_vol`) 도입 → 컬럼 정렬(우측, 등락률 색상)
5. 선택 종목 요약 패널(종가/대비/등락률/거래량/시장) 복구
6. 미사용 코드(`_fetch_and_render_market`, `_render_market_ui`) 제거 또는 역할 재정의

**복구 원본 확보 (중요)**

- 초기 커밋 `5bd7968`의 `10yp_2.py`에는 **동작하는 증권 화면 구현이 존재**한다.
  - `def setup_market_view` (1640행), `self._market_tree = ttk.Treeview(...)` (1662행), `def _search_market` (1687행)
  - 추출본 `Temp/10yp_2_initial.py` (169,703자, **컴파일 OK**)
- 따라서 M-1/M-2는 "새로 작성"이 아니라 **복원 + 개선**이 정답이며, 리스크가 크게 낮아진다.
- 다만 초기 구현도 5열(심볼/종목/가격/변화/비율)과 영어식 라벨이므로, 복원 시 **7열 한국어 라벨과 숫자 포맷**으로 개선해 적용한다.

---

### 2-D. 가계부 도메인 (개인 재무 도메인 전문가)

**현황**
- 뷰 1종: 월 선택 + 월별 요약(총수입/총지출/순저축) + 입력 폼 + 내역 Treeview
- 스키마: `transactions(id, date, type, category, amount, memo, saved_at)`
- API: `add_transaction` / `get_transactions` / `delete_transaction` / `get_month_summary` / `get_category_summary`
- 실사용: 3건

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| L-1 | **카테고리가 자유 문자열** — 마스터 테이블 없음. "교통", "교통비", "버스"가 각각 집계되어 통계 신뢰 하락 | 스키마 `category TEXT`, `categories` 테이블은 학습용 | **P1** |
| L-2 | 지출 **예산 대비 초과 경고 없음** | 예산 테이블 부재 | P1 |
| L-3 | **반복 거래(월세, 구독, 공과금) 없음** — 매월 수동 입력 | 스키마 부재 | P1 |
| L-4 | **엑셀 내보내기와 가져오기 없음** (일정, 기념일에는 있음) | 내보내기 핸들러에 가계부 없음 | P1 |
| L-5 | 그래프와 추이(월별 막대, 카테고리 파이) 없음 | UI에 canvas 없음 | P2 |
| L-6 | 시작 리마인더가 "이번 달 지출"만 보여주고 **수입 대비 잔액이 없음** | `show_startup_reminder` | P2 |
| L-7 | 테스트는 존재(`test_month_summary`)하나 **UI 경로 테스트 없음** | `tests/test_core.py:256` | P2 |

**해야 할 일**
1. `ledger_categories(kind, name)` 마스터 + 입력 폼을 **Combobox 고정**으로 변경 → 오타와 중복 집계 차단
2. 예산(`ledger_budget(ym, category, amount)`) + 초과 시 색상 경고
3. 반복 거래(`ledger_recurring(day, type, category, amount, memo)`) → 앱 시작 시 당월 미생성분 자동 반영(확인 후)
4. 가계부 엑셀 내보내기와 가져오기 (일정 도메인의 **손상 없는 패턴으로** 구현 — S-1/S-2 재발 방지)
5. "고정비, 변동비, 저축" 3분류 요약을 월 요약에 추가

---

### 2-E. 뉴스 및 정보 도메인 (미디어 수집 및 요약 엔지니어)

**현황**
- 42 소스 / 16 카테고리, 표준 라이브러리만 사용(`urllib` + `xml.etree`)
- 백그라운드 수집 스레드 + 상태 라벨 + 저장(`saved_news` 4건) + 키워드 분석(`news_summarizer.format_report`)
- 읽음 표시(`news_read`) 지원

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| W-1 | **소스 오분류와 중복** — "한겨레(교육)"이 `hani.co.kr/rss/health/`(건강) URL이고, "한겨레(건강)"도 **동일 URL** → 교육 카테고리에 건강 기사 유입 + 중복 수집 | `core/news_fetcher.py:59` 와 `:65` | **P1** |
| W-2 | **"오마이데일리"가 이탈리아 사이트**(`osservatoreroma.it`)를 가리킴 → "문화" 카테고리 오염 | `core/news_fetcher.py:57` | **P1** |
| W-3 | **RSS가 아닌 HTML 페이지를 소스로 등록** — "조선일보 스포츠"는 `...journallist_21.html` | `core/news_fetcher.py:63` | P1 |
| W-4 | **URL 인코딩 불일치** — Google 소스 다수는 퍼센트 인코딩인데 "Google 여행"은 **한글 원문**(`q=여행`) | `core/news_fetcher.py:31,33,34,68` | P1 |
| W-5 | **수집 실패가 조용함** — 42개 중 몇 개가 실패했는지 UI에 표시되지 않음 | 상태 라벨에 실패 수 없음 | **P1** |
| W-6 | 소스별 타임아웃과 동시 수집 정책 부재 → 순차 수집 시 체감 지연 | `news_fetcher` 수집 방식 | P2 |
| W-7 | 저장 뉴스 4건으로 실사용 미미, **자동 정리(보관기간) 없음** | DB 실측 | P2 |

**해야 할 일**
1. 소스 표 **전수 재검증**: (a)URL 도달성 (b)RSS 여부 (c)카테고리 일치 — 결과를 표로 남기고 잘못된 4건 즉시 교정
2. 수집 결과를 "성공 n, 실패 m (실패 소스 목록)"으로 **상태 라벨에 표시**
3. 소스별 타임아웃 적용 + 동시 수집(스레드 풀)으로 수집 시간 단축
4. 저장 뉴스에 90일 자동 정리 옵션

---

### 2-F. 설정 및 데이터 인프라 (데스크톱 데이터 무결성 전문가)

**현황**
- `core/config_manager.py`: `config.json` 경유 API 키, 테마, 리마인더
- 백업과 복원: `_backup_data` / `_restore_data` → `backup/` 폴더
- 로그: `core/logger.py` → `archive/logs/app.log` (1MB 회전, 3개 보관)
- 예외 계층: `core/exceptions.py` (`handle_error`, `safe_call`, `retry_on_error`)

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| I-1 | **`config.json`과 DB가 버전관리 제외** → 사고 시 복구 수단이 로컬 백업뿐 | `.gitignore:2-3` | **P1** |
| I-2 | 백업이 **수동 실행만** → "언제 마지막 백업인지" 알 수 없음 | 시작 시 백업 없음 | **P1** |
| I-3 | **소스 손상(mojibake) 37줄**이 `db_manager.py`에 존재(런타임 코드 4줄 포함) | `Temp/diag_final.json` | **P0** |
| I-4 | 신규 DB 생성 시 **깨진 기본 범주**가 저장될 수 있음 | `db_manager.py:223` | **P1** |
| I-5 | 로그 파일 한글 표기 불안정(터미널 인코딩 영향) | `app.log` 관측 | P2 |
| I-6 | DB 무결성 검사(`PRAGMA integrity_check`)와 스키마 버전(`user_version`) 없음 | 코드 부재 | P2 |

**해야 할 일**
1. `db_manager.py` **손상 문자열 전수 교정**(37줄) - 런타임 4줄(438/448/460/464)은 반드시 코드 동작과 함께 수정
2. 시작 시 **자동 백업(일 1회, 최근 7개 유지)** + 설정 탭에 "마지막 백업 시각" 표시
3. `PRAGMA user_version` 도입 → 마이그레이션 이력 관리
4. 백업 폴더를 외부 경로로 지정하는 설정 추가

---

### 2-G. 아키텍처 및 품질 (데스크톱 GUI 아키텍트 및 QA)

**현황**
- `10yp_2.py` **단일 파일 3,465줄**에 9개 뷰와 팝업과 도메인 로직이 혼재
- `ui/` 분리 1단계만 완료(`daily_editor.py`), `core/` 19모듈
- 테스트 34개(코어 중심), UI 테스트 0

**문제**

| ID | 문제 | 근거 | 등급 |
|---|---|---|---|
| A-1 | **증권 탭에 상호 배타적 구현 3개**가 공존 → 어느 것이 진실인지 불명 | 2-C M-6 | **P0** |
| A-2 | `db_manager.py`에 **죽은 정의 2개**와 DDL 중복 → 수정 시 어느 쪽을 고쳐야 하는지 혼선 | S-4, S-5 | P1 |
| A-3 | 거대 단일 파일 → 검토, 테스트, 충돌 위험 높음 | 3,465줄 | P1 |
| A-4 | **UI 자동 테스트 0** → 뷰가 조용히 폴백해도 아무도 모름(이번 M-1이 실제 사례) | 스모크 프로브 없음 | **P1** |
| A-5 | 폴백이 `except Exception: pass` 형태로 **원인 은폐**(예: 1692행) | `setup_market_view` | P1 |
| A-6 | 파일 인코딩 규약 미문서화 → 손상 재발 | 규약 문서 없음 | P1 |

**해야 할 일**
1. `ui/market_view.py` 등 **뷰 단위 분리**를 증권부터 재개(가장 손상이 큰 영역)
2. 예외를 삼키는 곳은 **`log.exception` 필수**로 바꿔 조용한 실패 금지
3. **뷰 스모크 테스트를 테스트 스위트에 편입**(`Temp/smoke_views.py` 로직을 `tests/test_ui_smoke.py`로 승격, `_market_widget`과 `_market_tree` 존재 검증 포함)
4. 인코딩 규약 확정: 모든 소스 **UTF-8(BOM 없음)**, 줄바꿈 LF, 커밋 전 전수검사 통과를 필수로

---

## 3. 결함 우선순위 총괄 (오늘 확정)

### P0 - 즉시 (데이터 손실 또는 기능 무력화)

| ID | 영역 | 내용 | 완료 기준 |
|---|---|---|---|
| P0-1 | 증권 | `setup_market_view` 안에서 **Treeview 직접 생성** → `_market_tree` 확보 | `hasattr(app,"_market_tree")` True, 검색 시 행 표시 |
| P0-2 | 증권 | 저장/수정/삭제/선택 핸들러의 `AttributeError` 제거 | 4개 버튼/이벤트 클릭 시 예외 0 |
| P0-3 | 스케줄러 | 엑셀 **내보내기-가져오기 왕복 복구** (컬럼명 정정 + 검증 목록 일치) | export→import 후 일정 수 동일 |
| P0-4 | 데이터 | `db_manager.py` mojibake **37줄 교정**(런타임 4 포함) | 손상 라인 0 |
| P0-5 | 증권 | `ui/market_widget` 의존 제거(부재 모듈 참조 정리) | 폴백 라벨 미표시 |

### P1 - 이번 주 (신뢰성과 사용성)

| ID | 영역 | 내용 |
|---|---|---|
| P1-1 | 증권 | 검색을 백그라운드 스레드로 전환(UI 정지 제거) |
| P1-2 | 증권 | Mock(가짜) 데이터 기본 비활성 + 경고 표시 |
| P1-3 | 뉴스 | 소스 4건 교정(교육=건강 URL, 오마이데일리, HTML 등록, 한글 URL) |
| P1-4 | 뉴스 | 수집 성공/실패 수 표시 |
| P1-5 | 학습 | `study_progress`, `study_subject_data` 경로 확인과 진도 위젯 |
| P1-6 | 가계부 | 카테고리 마스터 + 예산 경고 |
| P1-7 | 인프라 | 시작 시 자동 백업 + 마지막 백업 시각 표시 |
| P1-8 | 품질 | 뷰 스모크 테스트 승격 + `log.exception` 적용 |

### P2 - 다음 사이클

| ID | 영역 | 내용 |
|---|---|---|
| P2-1 | 스케줄러 | 죽은 정의(256-286행)와 DDL 중복 정리 |
| P2-2 | 가계부 | 반복 거래, 그래프, 3분류 요약 |
| P2-3 | 증권 | 지수/상품/기업 API 재신청(data.go.kr) |
| P2-4 | 아키텍처 | `ui/` 뷰 단위 분리 2단계 |
| P2-5 | 인프라 | `user_version`, `integrity_check`, 백업 경로 설정 |

---

## 4. 일정 (2026-09-18 시작, 9일)

> 기준일 2026-09-18은 **금요일**. 하루 1~2시간 작업 가정.

### Day 1 - 2026-09-18 (금) : 기반 정리 (P0-3, P0-4)
- [x] 진단 문서 작성 및 커밋 (`5d481b5`)
- [ ] `db_manager.py` mojibake 37 교정 (438/448/460/464는 코드와 동시 수정)
- [ ] 엑셀 왕복 복구: `df.columns` 정정 + `import_schedules_from_df` 키 정정 + `required` 목록 일치
- [ ] `tests/test_core.py`에 엑셀 왕복 테스트 1건 추가
- 검증: `py_compile` 통과, `unittest` 35개 통과, export→import 실측

### Day 2 - 2026-09-19 (토) : 증권 P0 복구 (P0-1, P0-2, P0-5)
- [ ] `setup_market_view`에서 Treeview(컬럼 7개)와 스크롤바 생성 (복구 원본: `Temp/10yp_2_initial.py` 1640-1687행)
- [ ] `_market_tree` 기반으로 저장/수정/삭제/선택 핸들러 정리
- [ ] `ui.market_widget` import 제거(부재 모듈 참조 삭제)
- 검증: 뷰 스모크에서 `_market_tree` 존재 + 검색 결과 행수 > 0

### Day 3 - 2026-09-20 (일) : 증권 화면 품질 (P1-1, P1-2)
- [ ] 검색 스레드화 + `after(0, ...)` UI 갱신
- [ ] 숫자 포맷 헬퍼(가격 천단위, 부호, 퍼센트) + 우측 정렬 + 등락 색상
- [ ] 선택 종목 요약 패널(종가/대비/등락률/거래량/시장)
- [ ] Mock 폴백 경고 표시(빨강) 또는 비활성
- 검증: 검색 중 UI 응답 유지, 표 컬럼/정렬 확인

### Day 4 - 2026-09-21 (월) : 증권 마감 + 테스트 승격 (P1-8)
- [ ] `tests/test_ui_smoke.py` 추가(9개 뷰 + 증권 필수 위젯 검증)
- [ ] 증권 미사용 코드(`_fetch_and_render_market` 등) 정리
- 검증: 전체 테스트 통과, 커밋 및 푸시

### Day 5 - 2026-09-22 (화) : 학습 도메인 (P1-5)
- [ ] `study_progress` / `study_subject_data` 호출 경로 확인
- [ ] 공부 탭 상단에 과목별 진도율 위젯
- 검증: 진도 기록 후 재실행 시 유지

### Day 6 - 2026-09-23 (수) : 뉴스 도메인 (P1-3, P1-4)
- [ ] 소스 표 전수 점검 및 4건 교정
- [ ] 수집 성공/실패 수 상태 표시
- 검증: 새로고침 시 성공/실패 집계 표시, 교육 카테고리 오염 해소

### Day 7 - 2026-09-24 (목) : 가계부 (P1-6)
- [ ] 카테고리 마스터 + Combobox 고정
- [ ] 예산 설정 + 초과 경고
- 검증: 카테고리 통계 일관성 확인

### Day 8 - 2026-09-25 (금) : 인프라 (P1-7, P2-5)
- [ ] 시작 시 자동 백업(일 1회, 7개 보관) + 마지막 백업 시각
- [ ] `PRAGMA user_version` + `integrity_check`
- 검증: 백업 파일 생성/회전 확인

### Day 9 - 2026-09-26 (토) : 회귀 테스트와 마무리
- [ ] 전체 테스트 + 스모크 + 수동 시나리오 점검
- [ ] 진단 문서 상태 갱신(P0/P1 완료 표시)
- [ ] 릴리스 태그 및 푸시

---

## 5. 완료 정의(DoD)와 검증 방법

### 5-1. 모든 변경에 공통 적용

1. `python -m py_compile 10yp_2.py core/*.py` 통과
2. `python -m unittest discover -s tests -t .` 통과 (기존 34개 + 신규)
3. 뷰 스모크 9종 예외 0 (`Temp/smoke_views.py` 방식)
4. mojibake 스캔 결과 증가 0 (`Temp/diag_final.py` 방식)
5. 변경 후 `Temp/` 산출물에 결과 기록(JSON 또는 텍스트)

### 5-2. 도메인별 추가 검증

| 도메인 | 검증 방법 |
|---|---|
| 스케줄러 | export 후 import 왕복 시 일정 건수와 내용 동일 |
| 증권 | 검색 결과 행수 > 0, 저장/수정/삭제 클릭 시 예외 0, 검색 중 UI 응답 |
| 학습 | 진도 입력 후 재실행 시 값 유지 |
| 뉴스 | 새로고침 시 성공/실패 집계 표시, 카테고리별 소스 정합 |
| 가계부 | 동일 범주 통계 합계 일치, 예산 초과 경고 표시 |
| 인프라 | 백업 파일 생성과 회전, `integrity_check` = ok |

### 5-3. 커밋 규칙

- 커밋 메시지에 결함 ID 명시: `[증권] Treeview 직접 생성 (P0-1)`
- P0 완료 시 이 문서의 체크박스를 갱신해 같은 커밋에 포함

---

## 6. 리스크와 결정 필요 사항

| ID | 항목 | 선택지 | 권장 |
|---|---|---|---|
| D-1 | 증권 Mock 폴백 | (A)유지 (B)경고만 (C)완전 제거 | **B** - 오프라인 데모 가치 유지 + 오인 방지 |
| D-2 | 증권 화면 구현체 | (A)`10yp_2.py` 통합 (B)`ui/market_view.py` 분리 | **B** - 분리 2단계와 함께 처리 |
| D-3 | 지수/상품/기업 API | (A)data.go.kr 재신청 (B)미사용 기능 제거 | **A(신청) + B(임시 숨김)** |
| D-4 | 문서 파일명 | (A)한글 유지 (B)영문 ASCII | **B** - 도구 인코딩 손상으로 파일명 오타 발생 이력 |
| D-5 | 백업 위치 | (A)로컬 `backup/` (B)외부 동기화 | **A + 선택적 B** |
| D-6 | 거대 파일 분리 범위 | (A)증권만 (B)9개 뷰 전체 | **A** - 단계적 |

### 6-1. 오늘 확인된 환경 리스크 (중요)

- **한글 문자열과 파일명 손상**: 문서 작성 도구에서 일부 음절이 간헐적으로 누락되는 현상이 실측됨 -> **ASCII 파일명 사용**(D-4)과 **작성 후 읽기 검증**을 규약으로 고정
- **콘솔 환경 불안정**: 파이프라인과 장시간 명령이 자주 실패 -> **스크립트 파일 + JSON 산출물** 방식 유지
- **Git 경로 변경**: GitHub Desktop 업데이트로 `app-3.6.5` 에서 `app-3.6.6` 으로 변경 -> 스크립트의 Git 경로 상수 갱신 필요
- **남은 변경 없음**: `10yp_2.py` 변경분은 커밋 `5d481b5` 로 원격에 반영 완료

### 6-2. Git 저장소 상태 (2026-09-18 실측 및 조치)

| 항목 | 실측 | 조치 |
|---|---|---|
| 현재 브랜치 | `main` | - |
| 원격 | `https://github.com/nana231dev/My_Scheduler.git` | - |
| 로컬 저장소 | `origin/main` 과 완전 동일 (차이 없음) | 동기화 완료 |
| 원격과 차이 | **차이 없음**(동기화 완료) | `10yp_2.py` 코드 차이 **없음** |
| 로컬 변경 커밋 | `5d481b5` (진입점 복구 + 진단 문서), `63eb501` (문서 보강) | 완료 |
| 엑셀 자산 | `기념일.xlsx` -> `Temp/backup_기념일.xlsx` 백업 | 완료 |

**동기화 완료 (2026-09-18)**: `git pull --rebase` 후 `git push` 성공 (`2da4f08..63eb501`, 로컬과 원격 일치).
원격 커밋의 `기념일.xlsx` 파일 제거가 반영되어 작업 폴더에서 해당 파일이 사라졌다(필요 시 `Temp/backup_기념일.xlsx`에서 복원).

### 6-3. 이전 세션 기록과 실제 상태의 차이 (중요)

이전 세션 요약에는 "3개 커밋을 푸시했다"고 기록되어 있으나, **현재 로컬 저장소에는 초기 커밋 1개만 존재**한다.
또한 이전 세션이 "증권 화면을 개선했다"고 한 편집은 실제로는 **동작하던 Treeview 구현을 제거**한 변경이었다.
(근거: `Temp/git_commit_check.txt` - 커밋 diff에서 `_market_tree = ttk.Treeview(...)` 생성 블록이 삭제)

교훈: 앞으로 화면 개선은 **스모크 검증(`_market_tree` 존재 + 행수 > 0) 통과 시에만** 커밋한다.

---

## 7. 다음 착수 (제안)

Day 1을 오늘 바로 처리하는 것이 가장 효율적이다. 이유는 다음과 같다.

1. **P0-4(손상 교정)** 는 이후 모든 작업의 기반이고, 다른 파일로 번지기 전에 막아야 한다.
2. **P0-3(엑셀 왕복)** 은 사용자 데이터(일정)를 직접 다루는 기능이라 손실 위험이 가장 크다.
3. 증권 P0(Day 2)는 코드가 크지 않아 하루면 끝나고, 그 뒤 "보기 좋게"가 자연스럽게 이어진다.

> 문서 상태: **2026-09-18 진단 확정 / Day 1 착수 대기**
