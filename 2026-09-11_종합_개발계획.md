# 🎯 My_Scheduler 종합 개발 계획 (2026-09-11 수립)

## 0. 사용자 핵심 요구 5가지

| # | 요구 | 현재 상태 | 목표 |
|---|---|---|---|
| 1 | 일정 정리 | ✅ 월간/주간/일간, 기념일, D-Day, 연간 진행률 | 리마인더/백업 강화 |
| 2 | 학습/공부 | ✅ 공부 탭(노트·단어·용어·진도), 단어장, 생활용어 | AI 연동, 복습 시스템 |
| 3 | 뉴스/정보 | ✅ 21소스·9분야, 검색, 저장/삭제 | 관심분야 맞춤 알림 |
| 4 | 증권 정보 | ✅ 종목 검색·저장·상세·차트(Mock) | 실데이터 연동, 투자 다이어리 |
| 5 | **가계부** | ❌ **미구현 (신규)** | 수입/지출 관리, 월별 통계 |

---

## 1. 최우선 — 가계부 탭 신설 (신규 핵심)

### 목표
- 수입/지출 기록 → 월별 지출 분석 → 예산 대비 통계

### DB 스키마 (`core/db_manager.py` 추가)
```sql
CREATE TABLE IF NOT EXISTS transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT,                 -- 2026-09-11
    type TEXT,                 -- 수입 / 지출
    category TEXT,             -- 식비/교통/통신/주거/학습/건강/문화/기타
    amount INTEGER,
    memo TEXT,
    saved_at TEXT
);
```

### UI (사이드바 '💰 가계부' 메뉴)
- **입력 폼:** 날짜(기본 오늘)·수입/지출·카테고리·금액·메모 → 추가/삭제
- **월별 집계 라벨:** 이번 달 총수입 / 총지출 / 순저축
- **Treeview 목록:** 날짜·구분·카테고리·금액·메모 (월 필터 콤보)
- **간단 막대/파이 차트:** 카테고리별 지출 비중 (Canvas로 자체 구현)

### 핵심 메서드
- `add_transaction`, `get_transactions(month=None)`, `delete_transaction`
- `get_month_summary(year, month)` → 수입/지출/순액

---

## 2. 1순위 — 일정 활용 강화

### 2-1. 데이터 백업/복원 (설정 탭)
- 버튼 1: `scheduler.db` + `config.json` → `backup/` 폴더로 복사 (타임스탬프 파일명)
- 버튼 2: 최근 백업 파일 선택 → 복원 (앱 재시작 안내)

### 2-2. 알림/리마인더
- **오늘 일정 & D-Day & 지출 목록**을 앱 시작 시 요약 팝업
- 옵션: `messagebox`(간단) → 향후 `plyer`/시스템 알림 확장

---

## 3. 2순위 — 학습 강화

### 3-1. AI 비서 (Gemini 키 이미 보유)
- 설정 탭에 Gemini API 키 입력란 추가
- 공부 탭/단어장에 "AI 도움" 버튼:
  - 단어 예문 생성 / 뜻 설명 / 생활용어 번역
- `core/ai_helper.py` 신규 (간단 REST 호출)

### 3-2. 복습(스페이스 반복) 시스템
- 단어장에 `reviewed_at`, `ease` 필드 추가
- "오늘 복습할 단어" 자동 선별 (간격: 1·3·7·14일)

---

## 4. 3순위 — 뉴스/증권 활용 확장

### 4-1. 뉴스
- 저장 뉴스 → "이메일/알림 요약" (선택)
- 분야별 북마크 폴더링 (이미 카테고리 필드 있음)

### 4-2. 증권 실데이터
- 공공데이터포털(금융위원회) `GetStockSecuritiesInfoService_V2 / getStockPriceInfo_V2` 실응답 확인
- 실데이터 호출 + Mock 폴백 구조 적용, 투자 일기 연계
- **투자 일기:** 종목별 매수/매도 기록 → 손익 계산 (가계부와 연계 가능)

---

## 5. 4순위 — 통합/편의/배포

### 5-1. 통합 검색 (Ctrl+F)
- 일정·노트·단어·생활용어·뉴스·증권·가계부 한 번에 검색
- `core/search.py` + 사이드바 검색창

### 5-2. 단일 exe 배포
- `pip install pyinstaller`
- `pyinstaller --onefile --windowed 10yp_2.py` (아이콘 포함)

---

## 6. 개발 로드맵 (2026-09-12 갱신 — 다른 PC 이력 반영)

| 단계 | 내용 | 상태 |
|---|---|---|
| **Phase 0** | **코드 동기화** — 다른 PC 리팩토링 버전(core+ui 31파일) 가져와 병합 | 🆕 **최우선** |
| **Phase 1** | 가계부 탭 (DB+UI+통계) — 다른 PC 버전에 없음 → 병합 후 구현 | 진행 예정 |
| **Phase 2** | 백업/복원 + 시작 요약 알림 (다른 PC에서 부분 완료 → 이관 후 검증) | 조건부 |
| **Phase 3** | AI 비서 + 단어 복습 시스템 (i18n/formulas 병합 후 확장) | 유지 |
| **Phase 4** | 통합 검색 (다른 PC `search_view` 이관 확인) + 증권 실데이터 | 유지 |
| **Phase 5** | exe 빌드 + 투자 일기 | 유지 |

---

## 7. 기술 메모
- 파이썬 3.14 / ttkbootstrap darkly / SQLite / 표준 urllib
- 새 기능은 기존 패턴 유지:
  - DB: `core/db_manager.py`에 테이블+CRUD 추가
  - UI: `10yp_2.py`에 `setup_<view>_view()` + 사이드바 버튼 + `switch_view` 케이스
  - config: `core/config_manager.py` 경유
- 검증: `verify_*.py` 작성 → `archive/` 보관

---

# 🔄 8. 다른 PC 진행 이력 반영 (2026-09-12 추가 — 사진 기반)

> 출처: `Temp/` 폴더의 다른 PC 진행 현황 사진 (2026-09-10 기준 정리 문서)

## 8-1. 다른 PC에서 완료된 내용 (우리 로컬에 없음 ⚠️)

### 모듈 분리 리팩토링 (09-04~09-07)
- `10yp_2.py` 단일 파일 → **`core/` + `ui/` 패키지 31개 파일, 약 13,000줄**로 분리
- `core/`: db_manager, news_fetcher, market_fetcher, **quote_fetcher**, word_data, phrase_data, config_manager, solar_terms
- `ui/`: daily_view, weekly_view, monthly_view, news_view, market_view, study_view, wordbook_view, phrase_view, **search_view, report_view**, settings_view, editors, main_window

### 핵심 기능 개선 (09-08)
- [x] 뉴스 읽기 표시 (읽음/안 읽음 체크, 색상 구분, 격수 표시)
- [x] 명언 즐겨찾기 (`favorite_quotes` DB 테이블, ⭐/🗑 버튼)
- [x] 주간/월간 뉴스 요약 (날짜별/분야별 그룹화, Notebook 탭)
- [x] 뉴스 알림 기능 (위젯 매칭, ToastNotification)
- [x] 버그 수정 (editors.py IndentationError, db_manager VALUES 플레이스홀더, RSS 팀명 오타)

### UI 개선 (09-09)
- [x] 일정관리 시스템 (사이드바, 1개월 데이터 로드)
- [x] 뉴스 날짜 별빛 강색 (시작일~종료일 필터)

### 학습 도구 확장 (09-10) ✨ 신규
- [x] **수학/과학 공식집** (`core/formulas.py`): 160공식 (초등/중등/고등/대학, `level` 속성)
  - 기존 분야: 산수·대수·미적분·통계·수열·조합론, 물리화, 전자공학, 에너지, 화학, 정보처리
  - 신규 수학: 중등수학(13), 고등수학(20), 대학수학(18)
- [x] **과학/공학 용어집** (`core/glossary.py`): 100+ 용어 (원소, 물리상수, 과학용어, 공학법칙)
- [x] **문서 요약** (`core/doc_summarizer.py`): PDF/DOCX/XLSX/PPTX 큐칭기반 추출 요약
- [x] **다국어 지원** (`core/i18n.py`): ko/en/ja
- [x] **뉴스 키워드 요약** (`core/news_summarizer.py`): 키워드 매칭, 트렌딩 키워드
- [x] **ICS 캘린더 동기화** (`core/cloud_sync.py`): ICS 내보내기/가져오기

### 품질 개선 — 기술 부채 해결 7/7 완료
- [x] Pylance Mixin 타입 힌트: `ui/common.py` ViewMixin 기본 클래스 + 10개 속성 선언
- [x] 에러 핸들링 표준화: `core/exceptions.py` (safe_call, handle_error, retry_on_error, DatabaseError/NetworkError/ConfigError/FileParseError)
- [x] 테스트: `tests/test_core.py` — **33개 테스트 pytest 모두 통과**
- [x] 로깅: `core/logger.py` (모든 core 모듈 print → logging, INFO/WARNING/ERROR/CRITICAL)
- [x] Google News 다국어 (ko/en/ja) + 키워드 필터
- [x] 정규식 패턴 검색 요약 수정
- [x] Google News 검색 기반 URL 대체 (연합/경제/IT과학 3개 소스)

## 8-2. 갭 분석 — 로컬 실제 확인 (2026-09-12)

| 항목 | 로컬 상태 |
|---|---|
| `ui/` 패키지 (13개 뷰 파일) | ❌ **없음** |
| `tests/test_core.py` (33개 테스트) | ❌ **없음** |
| `core/formulas.py` (160공식) | ❌ **없음** |
| `core/glossary.py`, `doc_summarizer.py`, `i18n.py`, `news_summarizer.py`, `cloud_sync.py`, `quote_fetcher.py`, `exceptions.py`, `logger.py` | ❌ **없음** |
| `core/` 8모듈 (db/news/market/word/phrase/config/solar) | ✅ 있음 (우리 로컬 버전) |
| `10yp_2.py` 단일 파일 (~2,000줄, 가계부 진행 전) | ✅ 있음 |
| `core/news_summarizer.py` (2026-09-12 재구축) | ✅ 있음 (로컬 신규 작성 + 앱 연동 완료) |

→ **두 버전이 갈라짐(split).** 다른 PC 버전(31파일)이 우리 로컬 버전(단일 파일)보다 훨씬 앞서 있음.

# 🏗 9. 코드 재구축 진행 (2026-09-12 — 복사 불가 → 신규 작성 결정)

> 복사가 어려워 **md 문서를 기반으로 기능적으로 동등한 코드를 새로 작성**하기로 결정.
> 작동 중인 `10yp_2.py`는 유지하고, 없는 모듈을 추가하는 방식(Plan A).

## 9-1. 완료 (이번 세션)

| 모듈 | 내용 | 검증 |
|---|---|---|
| `core/logger.py` | print → logging 전환, 콘솔+파일(archive/logs/app.log) | ✅ 테스트 2건 |
| `core/exceptions.py` | SchedulerError 계층(Database/Network/Config/FileParse) + safe_call/handle_error/retry_on_error | ✅ 테스트 4건 |
| `core/i18n.py` | ko/en/ja 40키 UI 문자열 + t() 헬퍼 | ✅ 테스트 3건 |
| `core/formulas.py` | **160공식** (산수~대학수학 9분야 + 물리화/전자공학/에너지/화학/정보처리), level 필터·검색 API | ✅ 테스트 4건 |
| `core/glossary.py` | **100용어** (원소20/물리상수15/과학용어35/공학법칙30), 검색 API | ✅ 테스트 3건 |
| `tests/test_core.py` | unittest+pytest 양립 테스트 스위트 | ✅ **21/21 통과** |
| `10yp_2.py` | `setup_logging()` 앱 시작 연동 | ✅ py_compile RC=0, 임포트 OK |
| `10yp_2.py` 공식집/용어집 UI 탭 | 사이드바 "📐 공식집"·"🔬 용어집" 버튼 + Treeview 뷰 (주가·분야 필터 + 검색 + 전체보기 + 건수 표시), switch_view 케이스 추가 | ✅ py_compile RC=0, 임포트 OK, bare StringVar 0건 |

## 9-2. 다음 재구축 목록 (md 기준 잔여)

1. `core/news_summarizer.py` — 뉴스 키워드 매칭 요약 + 트렌딩 키워드 (✅ 2026-09-12 완료: `core/news_summarizer.py` 재구축 + `10yp_2.py` `show_news_keywords` 팝업 연동, `format_report`/`trending_keywords`/`filter_by_keyword`/`tfidf_keywords` 포함)
2. 명언 기능 — `favorite_quotes` DB 테이블 + ⭐ 즐겨찾기/🗑 버튼 (quote 소스는 뉴스 요약과 함께)
3. 뉴스 읽음 표시 — `news_read` 필드/테이블 + UI 체크·색상 구분
4. `core/cloud_sync.py` — ICS 캘린더 내보내기/가져오기
5. `core/doc_summarizer.py` — PDF/DOCX/XLSX/PPTX 텍스트 추출 요약 (의존성: pypython-docx 등 별도 설치)
6. ~~공식집/용어집 UI 탭~~ — ✅ 완료 (9-1 참조)
7. `tests/` 확장 — DBManager/뉴스/증권 테스트 추가 (21 → 30+건)

## 9-3. 결정 사항
- `ui/` 13파일 전면 분해(다른 PC 방식)는 **보류** — 단일 파일 유지가 안전 (뷰 분리는 안정화 후 검토)
- 테스트 기대값은 실제 데이터에 맞춤 (예: `대학` 19건 = 대학수학 18 + 통계 1)

## 8-3. 해야 할 내용 (우선순위)

1. **🆕 코드 동기화 (최우선)** — 다른 PC의 리팩토링 버전을 이 PC로 가져오기
   - 방법 A: 다른 PC에서 `git push` → 이 PC에서 `git pull` (권장)
   - 방법 B: 다른 PC 폴더 전체를 USB/클라우드로 복사 (git 없으면)
   - 주의: 이 PC의 최신 변경(뉴스 분야 9종, 증권 탭 상세/매매동향, config.json API키 시스템)이 **덮어써지지 않도록** 커밋 후 병합
2. **병합 후 검증** — `pytest tests/` 33개 통과 + 앱 실행 확인
3. **가계부 탭 구현** — 다른 PC 버전(31파일) 기준으로 구현 (`core/db_manager.py` + `ui/` 뷰 패턴)
4. **두 버전 기능 통합 확인** — 이 PC 신규 기능(증권 상세/매매동향, 뉴스 9분야, API키 JSON)이 다른 PC 버전에 있는지 확인하고 없으면 이관
5. 기존 로드맵 Phase 2~5 진행 (위 6장 갱신본 참조)
